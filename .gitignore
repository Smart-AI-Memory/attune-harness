__pycache__/
*.egg-info/
.pytest_cache/
.coverage
htmlcov/
build/
dist/
.venv*/
.pilot/
.benchmarks/
# Receipts stay local. The few that tests read are force-tracked (git add -f);
# Llama-derived ones are never redistributed and their tests skip when absent.
docs/receipts/
**/receipts/
examples/extensions/installed-example/
examples/extensions/qualified-example/
examples/recovery/installed-run/
examples/review/installed-run/
examples/review/run/
CLAUDE.md
.DS_Store
*.log
.env
.env.*
# Local workflow history and SQLite sidecars.
/.attune/history.db*
# Local session notes; never published. Same rules as the wip snapshot branch.
