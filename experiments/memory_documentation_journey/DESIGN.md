# Bounded memory documentation journey: local preparation

2026-09-17. Patrick authorized local preparation, selected-document repair and
temporary synthetic memories. Additional native calls require a concrete proposal
first. This experiment must not edit production adapters, the Spec collector,
existing memories, frozen comparison artifacts or unrelated work.

Inspected implementation: installed Attune AI 16.4.0 `file_stash.py`, SHA256
`9f97a33f16ca727e13aa42af52dea73415f4a24687cb0db1b32c0ce7e256391f`, identical to
the original source checkout. `search` adds a cwd score bonus; `recent` sorts cwd
matches first. Neither excludes foreign-project records. The existing real
adapter probe passed using an explicit temporary store. A scratch import of an
unchanged captured module returned Cedar then Elm from both methods in 0.0048s.
This is local behavior evidence, not native-model or UI latency.

The scoped search of README, docs, Harness memory sources and memory-worker
design found accurate descriptions, including the shared-memory baseline and
connected capability map. No selected production documentation mismatch was
found. Use clearly labeled disposable documents: an incorrect project-isolation
claim plus an unsupported guarantee about other backends; a correct control
describing ranking and explicitly leaving other backends unknown. The only repair
target is the incorrect document. No style preference becomes an acceptance rule.

Reuse installed review/fix/test tasks, command participants for a disclosed local
simulation, the corrected assessment criteria verbatim, the repair-to-test link,
real pytest, and the installed AI Spec workspace/grammar. Capture byte-identical
adapter source and its atomic-I/O dependency identity; execute real adapter code
against disposable memories. Tests cover both APIs, cross-project visibility,
ranking and no cwd match. The frozen repair probe executes the actual adapter;
and compares the document's JSON example of returned IDs to observed results;
the reviewer owns the meaning of the surrounding prose. No exact-wording
oracle constrains a valid paraphrase. Model judgment is not qualified by scripted
responses. The local command worker copies an example correction purely to test
the existing replacement transport.

The existing repair route requires an observed failing baseline probe. A behavior
probe alone already passes before a documentation repair, so it cannot qualify
that route. The explicit JSON example gives this documentation fixture a real,
deterministically checkable error: its predicted IDs exclude Elm. Parse only data;
never execute a model-written document. Passing that example check does not certify
the surrounding prose or the claim about other backends.

The executor explicitly selects the repair scope and publishes test evidence to
Spec. Probe freshness at both owners: mutate captured Python source after a
passing test and presented Spec form; attempt that exact old form. Also mutate
the Markdown reference before assessment acceptance. The assessment corpus
captures Markdown only, and Spec owns form revisions rather than source watching.
If the unrefreshed Spec form still accepts, retain the counterexample and block
the full journey claim. Do not repair that integration gap outside the authorized
documentation boundary or disguise a synthetic redo as automatic invalidation.

Acceptance requires a required correction of both bad assertions, the unchanged
control accepted with uncertainty intact, actual adapter tests, preserved
unrelated bytes, linked durable receipts, and source-driven rejection of the old
Spec decision. Native acceptance remains pending. Report first behavior finding,
first scripted assessment and gate-render times separately; do not call those a
measurement of the user's end-to-end wait.

Rejected: changing accurate production docs, implementing isolation, rerunning
the prior comparison, replacing Spec with a new gate, substituting a fake adapter,
or spending before resolving a demonstrated deterministic acceptance blocker.

Preparation reflection: the first reference put source identity before code and
exceeded the keyword route's 500-character excerpt. The revised reference includes
both exact source lines, observed results and the unknown within 500 characters;
every assessment packet is checked for the entire reference. The full module and
identity remain captured separately. An attempt to retain the runnable fixture
inside this repository inherited its parent pytest settings and was correctly
blocked. The fixture now carries its own captured `pytest.ini` to define the
qualified default discovery profile. These are disclosed preparation corrections,
not model failures or changes to production retrieval/discovery behavior.
