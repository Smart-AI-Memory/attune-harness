# Plan/build response-contract confirmation — September 18, 2026

**The approved 12-call confirmation is complete. The repaired contract accepted
12/12 original replies. Luna and Astra each produced four clean outcomes; Task 8
remains open for the connected native journey.** No reply was repaired to obtain
these results. Total estimated Codex usage was **30.29318 credits**, within the
approved 25–40 estimate and 50-credit planning ceiling.

## Outcomes and their meaning

| Requested model | Planner: clean / attempted | Worker: protected oracle passed / attempted | Contract valid |
|---|---:|---:|---:|
| Luna 5.6 | 2/2 | 2/2 | 4/4 |
| Astra 6 | 2/2 | 2/2 | 4/4 |
| Fable 5.1 | 1/2; other needs a minor factual qualification | 1/2; other fails actual CLI execution | 4/4 |

All six plans preserve the corrected goal: export every finding, including unknown
and refuted findings, and retain advisory notes. All six conform to the ordered
build profile, retain the protected checks and stage as unaccepted drafts through
the actual host. None introduces an unanswered routine design choice. All six
reject a source change before first staging; restoring the original fixture bytes
allows the unchanged proposal to stage. Staging preserves the original reply and
does not grant acceptance.

Five of six worker artifacts pass the protected CLI/serialization checks without
source or response changes. They preserve mixed-status findings, notes, empty
documents and default JSON. All five successful build receipts reject a later
source change. Every worker evaluation preserves protected inputs and unrelated
dirty fixture work. No critical or host-enforcement miss was observed in these
cases. This is bounded evidence, not a production reliability estimate.

The two findings differ substantially in severity:

- **Fable worker repetition 1, sample-04: implementation defect.** Its
  `isinstance(document, Documentation)` check rejects the class created when the
  documentation module runs as `__main__`. Both nonempty and empty CLI exports
  fail with `TypeError`. Direct mixed-status serialization and existing JSON
  compatibility still pass. The actual protected CLI oracle stops the build with
  `needs_revision`; no successful handoff is issued. This repeats a source-level
  failure from the earlier screen, after the response-interface defect is fixed.
- **Fable planner repetition 2, sample-07: low-severity factual rationale.** The
  selected implementation is sound, but its note asserts that printing each
  record separately would make the outputs diverge. A retained counterexample
  produces identical bytes. Qualify that statement as a possible maintenance
  risk, rather than an inevitable output difference. This is not a defective
  goal or an execution blocker. It prevents a strictly clean response score;
  it is not equated with the worker's functional failure.

The strict clean-response total is **10/12**. The summary's historical
`original_usable` field implements that strict count. Six of six plans remain
substantively aligned; one needs the small factual correction described above.
Correctly disclosed performance/environment unknowns were not defects. Imprecise
header wording in sample-10 was kept as optional advice because its surrounding
instructions explicitly retain separate complete finding records. No speculative
performance guarantee or unsupported stronger router is needed to explain the
successful cases.

## Execution, cost and timing

Patrick approved four calls each for Luna, Astra and Fable. Fresh read-only auth
checks confirmed ChatGPT login and Claude Max. The existing runner forced Standard
Codex service and ChatGPT routing, removed API-key overrides from child processes,
checked source/preparation/decision hashes before dispatch and enforced allocation
counts and credit reservations. All twelve native invocations completed; there
were no retries or fallback calls. The allocation is now exhausted.

| Requested model | Calls | Estimated Codex credits | Dispatch through validation, min / median / max |
|---|---:|---:|---|
| Luna 5.6 | 4 | 0.57568 | 21.21 / 25.10 / 29.82 s |
| Astra 6 | 4 | 29.71750 | 23.42 / 32.79 / 43.49 s |
| Fable 5.1 | 4 | Existing Claude Max | 29.25 / 47.71 / 106.24 s |

Credits are token-based estimates, not verified account deductions. Claude reports
a $2.67843825 list-price equivalent, not a verified API charge. No new API-dollar
allocation, credential change or active installation change was used.

The first returned worker validated in 23.50 seconds; its later local replay took
0.70 seconds and passed. These separate durations do not measure the user's wait
to see that finding. Desktop presentation and human-response timing remain unknown.
This candidate-only confirmation has no fresh baseline and makes no causal or
comparative-latency claim.

All eight Codex calls retained nonfatal skills-budget and unused MCP startup
warnings; no Codex tool-use event was observed. Their metadata did not report the
backend model name, so attribution is by requested model/argv. Claude reported
the requested model and two internal turns per native invocation, despite one
host exchange. Full startup context is not isolated or captured. The twelve-call
cap counts native invocations, not underlying provider turns.

## Assessment and retained evidence

The session assistant inspected and graded model-label-hidden packets before
opening the randomized mapping; grades were hashed first. This was not independent
review or strict blinding: sample-03 was inspected early during execution, making
its identity inferable from dispatch order. The other packets were first inspected
together after all calls completed. That limitation is retained in the grade file.

Actual worker evaluation reused the installed Spec collector, effect journal,
protected checks and stale-source handoff validation. The CLI integration driver
and reviewer were fixed fixtures. Plan evaluation reused actual planning/staging
owners with retained replies. No native critic/reviewer or build of the generated
plans ran. Local replay made no additional native calls.

All 769 files in the preceding Task 1–8, original native and contract-correction
receipt manifests remain unchanged. The final runner verification confirms the
approved packet and loaded wheel still match their bindings. Product source and
live memories were not changed for this confirmation.

Final lifecycle checks pass, including 77 cited source/evidence tokens. The first
invocation omitted its change list and conservatively returned a chair-review
requirement for unknown scope. That initial receipt is retained; supplying the
actual documentation/receipt paths resolves the classification without a waiver
or a new approval. The two retained local replay/summary scripts pass Ruff and
Black. Existing product suites were not rerun for this documentation and
experimental-evidence-only turn.

Evidence: [summary](receipts/plan-build-contract-confirmation-2026-09-18/summary.json),
[call ledger](receipts/plan-build-contract-confirmation-2026-09-18/live/ledger.json),
[frozen grades](receipts/plan-build-contract-confirmation-2026-09-18/blind-grades.json),
[grade hash](receipts/plan-build-contract-confirmation-2026-09-18/blind-grades-freeze.json),
[artifact/plan evaluations](receipts/plan-build-contract-confirmation-2026-09-18/evaluations),
[rationale counterexample](receipts/plan-build-contract-confirmation-2026-09-18/alternative-rationale-probe.json),
[transport audit](receipts/plan-build-contract-confirmation-2026-09-18/native-transport-audit.json),
[prior receipts](receipts/plan-build-contract-confirmation-2026-09-18/prior-receipts.json).

## Next bounded qualification

Retain Luna as the routine planner/worker candidate. The next useful test is one
connected native planner → critic → accepted dependent build → reviewer → protected
checks → human-control journey. Stronger review remains a candidate requiring
its own evidence, not a role qualified by Astra's planner/worker results here.
Include modification of an existing file using the host-supplied preimage, real
planner-to-worker and worker-to-reviewer handoffs, a deliberate adverse control,
interruption/resume without repeats, human correction and stale acceptance.

Prepare the exact cases, role assignments, call ceiling and cost locally before
requesting another allocation. Preserve the known bad worker as a reviewer/test
control instead of spending another call merely to obtain a passing replacement.
Do not make an unmeasured claim that a stronger router is necessary. Task 8,
native production adoption and release remain unaccepted.
