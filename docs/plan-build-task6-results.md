# Plan/build Task 6 — steering, controls and Spec

Tasks 1–6 are accepted. The actual Spec workspace automatically accepted Task 6
at revision 22 under Patrick’s existing non-high auto-run choice. The implementation
connects Spec collection to Harness intent and preserves completed work during
bounded correction. Qualification remains local.

The Spec form binds the current work checkpoint and supported controls. Evidence
alone grants nothing. The real collector rejects foreign, stale, replayed and
unconfirmed auto-run actions. Its response and accepted receipt are retained in
the work record. High-risk acknowledgment cannot grant build effects. An interrupted
acceptance write requires a fresh form instead of replaying a consumed nonce.

The captured JSONL fixture now uses this collector twice: before initial build
and after correcting the remaining steps. It preserves the first verified output,
completes the other two steps and carries all outputs into the existing pytest
handoff. Four actual scripted command calls ran. The protected oracle and unrelated
dirty work remain intact. A later source change is rejected. The first initial
worker result took 171 ms; build/correction wall time was 1.46 s. These are single
local backend observations, excluding initial form presentation, native inference,
desktop rendering and human response time.

Legacy import retains task descriptions, risks, dependencies and checks, discloses
unmapped content, and imports no approval. Reimport keeps the same work identity
while requiring acceptance of the changed plan. Planning freshness excludes only
a well-formed trailing Spec state comment; original bytes remain identified.
Effects still use the stricter complete checkout snapshot.

## Qualification record

- **777 distinct source checks pass:** the 775-check regression run plus the
  final 257-check current-source qualification and focused receipt-binding check.
  Overlapping checks are counted once; no skips or xfails.
- **776 installed checks pass**, plus a focused recheck of the strengthened
  receipt-binding assertion. Six changed Harness module hashes match the wheel.
  The actual optional Spec owner was loaded from its existing candidate source
  checkout; owner module paths and hashes are retained. Base imports remain
  dependency-free. The active MCP installation is unchanged.
- Changed-line coverage: new bridge 94.35%; other changed modules 88.1–100%.
  Coverage is local software evidence, not model quality.
- All five load-bearing guard removals are detected after isolating two masked
  guards. Initial 3/5 results and the corrected 5/5 evidence are both retained.
- Both lifecycle checks pass, covering 52 cited tokens; all prior Task 1–5
  receipt files remain unchanged.
- The actual captured-feature journey, form Markdown, collector responses,
  accepted/completed work records and pytest output are retained in
  `docs/receipts/plan-build-task6-2026-09-18/`.

## Corrections and limits

Initial bridge trials found that the Spec completion renderer assumes a local
planning artifact. The bridge now supplies a real external-work-record evidence
section using the existing grammar. An initial probe-only adjustment did not fix
that artifact assumption; the subsequent targeted adapter change did.

The new integration fixture initially used an incorrect test-intake keyword, and
one nested-import disclosure block was placed in the wrong function. Both were
corrected before qualification. Installed Spec regression tests initially lacked
a captured assessment fixture; that test fixture is now included. Earlier test
expectations also needed to reflect the Spec boundary's normalized stale-producer
error. Original failing outputs remain available.

Steering supports a verified, unchanged completed prefix and corrected pending
suffix. It preserves old journals and protects completed outputs. General goal
rebasing after effects, partial-step correction, arbitrary graph changes and
unsupported control phases remain blocked. One dedicated POSIX checkout and
trusted bounded check commands are qualified; this is not a security sandbox.

The bridge runs against the actual optional Spec owner in an isolated local
process. It does not upgrade the current MCP host. No independent model review,
provider-backed quality pipeline, paid/native trial, live installation, live
memory operation or release ran. Successful local transport and grammar checks
do not establish native model competence.
