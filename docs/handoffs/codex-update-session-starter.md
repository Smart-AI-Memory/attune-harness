# Completed: next bounded assessment correction

2026-09-17. Patrick authorized the selected correction and asked to incorporate
the pushback: reject unsupported certainty without making accurately disclosed
unknowns or optional details defects; use fresh cases rather than tune only to
Canada. See `docs/assessment-quality-correction.md` and the separate experiment
`experiments/assessment_quality/{correction.py,correction_v2.json}`.

Installed offline rehearsal: 16 fresh-case and 16 retained-case transport runs,
complete assessor/grader evidence, six controls per proposed grading packet,
zero provider calls. All 35 scoped checks plus Ruff/Black pass; all 384 previous
receipt files remain unchanged. Current
proposal and final checks are in
`docs/receipts/assessment-quality-correction-2026-09-17/prepared/`; the parent
directory retains the superseded first preparation. Do not modify the completed
trial's frozen files or treat injected replies as improved model judgment.

The concrete next allocation is 24 native calls (Astra8, Sol4, Fable12), expected
45–65 Codex credits with a 100-credit planning ceiling, existing Claude Max and
zero new API dollars. Patrick approved this exact allocation through the decision
form; the saved reply and live admission are under the correction receipt folder.
Forty offline checks qualify the controller. All 24 calls completed at 50.94182
estimated Codex credits. Revised candidate and baseline both score 8/8 on substantive
grading; all 48 control judgments pass. Lead review was frozen before grading and
agrees with all 16 decisions. Two outputs (one in each Fable arm) measure 351 by the
whitespace counter; length was tracked separately from semantic grading. O-17 logs
that observation. Preserve the completed ledger; do not replay either phase.

The revised experimental contract is supported, with no demonstrated advantage
over baseline on these four synthetic cases. Next useful qualification would test
realistic mixed evidence, implicit gaps and conflicting authority. No automatic
new campaign or production adoption is authorized. Original native Task8 remains
unaccepted. All 384 prior receipt files match; combined assessment-experiment
usage is 83 calls / 158.56568 estimated credits, not an account billing statement.

# Completed: bounded native assessment comparison

2026-09-17. Patrick approved 8 fresh cases, baseline/candidate instructions,
two lead families, up to 64 native invocations and 250 Codex credits, using the
existing Claude Max subscription and no new API-dollar allocation. The trial
finished at **59 calls / 107.62386 estimated Codex credits**. No production source
or active plugin changed. Original native Task 8 remains unaccepted.

Read `docs/assessment-quality-comparison-results.md` first. Full-narrative grades:
Fable baseline 7/8, candidate 5/8; Astra baseline 8/8, candidate 7/8. A disputed Fable
wording grade would raise its candidate to 6/8 without changing the rejection.
Both candidate profiles notice an unsupported Canada assertion but make
correction optional; both matched baselines require evidence or revision.
Do not adopt the failed candidate. The later correction above implements a narrower
fact-unknown versus unsupported-assertion distinction. No automatic
new provider campaign or promotion is authorized by the completed experiment.

Controller correction: first graders lacked real links/host checks, so initial
11 grades are retained but excluded. Stopped safely after 43 observed calls,
retained all 32 assessments, then froze a grading-only amendment: complete evidence
derived from saved native requests, two different cases per call, same independent
grader roles and original cumulative ceilings. Sixteen amended calls graded 32
outputs; 48 control items passed. Original runner bytes were restored after the
deliberate integrity-check stop. Original protocol/ledger and the lead review
remain frozen; no assessment was repeated. 19 offline checks plus Ruff/Black pass.

Artifacts: `experiments/assessment_quality/{trial.py,grade_repair.py,fixtures.json}`;
`tests/test_assessment_quality_trial.py`; receipts under
`docs/receipts/assessment-quality-2026-09-17/`, including `grading-amendment/`.
Preserve the original and amended protocols, discarded grades, reconciliation
and accounting. O-14/15/16 log evidence-delivery, evaluator-context and host-owned
verification opportunities. Severity/wording disagreements are explicit and do
not require Patrick's intervention merely to decide against promotion.

# Completed: connected journey qualification

2026-09-17. Patrick approved the three-step sequence: inventory intended
capabilities, qualify a representative installed journey, then extend checks and
make native follow-through concrete. All three are recorded complete through the
existing repaired installed Spec host; the plan retains exact executor receipts.
This continues authorized unattended execution, not a fabricated human response.
See `docs/connected-journey-qualification-results.md` and
`.claude/plans/connected-journey-qualification.md`.

Production addition: `test --from-task COMPLETED_REPAIR` binds a separately
accepted test task to current producer identity/checkpoint/artifact/probe and
actual replacement scope. Summary/journal probe and reviewer inconsistencies
found by independent review are repaired. Existing standalone test routes remain.
Assessment→repair scope selection and test→Spec publication remain explicit
executor handoffs; no automatic plan/build workflow is claimed.

Evidence: 29 new connected cases; 336 source / 333 installed core checks (three
source-only exclusions); memory107; Harness service392 installed (one additional
source-only exclusion and seven AI-host profile skips); code-host17 covers those
seven; A2A38 source and installed with permitted loopback fixture. Four guard
removals detected; independent review closed; 52 installed runtime modules match.
The candidate wheel is only in `/private/tmp/attune-connected-h_i2dp77/installed`.
No active plugin/install, original AI checkout, provider campaign or release changed.
Qualification scripts/profile staging issues are disclosed in the report.

Native Task8 **remains unaccepted**. Existing runner and corrected review contract
already implemented/executed; original60 remains48correct/revise; corrected8 is
separate. Original941 and corrected212 retained files still match. The next
native boundary is the assessment-quality contract and a prospective grading/
trial protocol, documented in
`docs/specs/connected-journey-qualification/native-follow-through.md`.
Do not repeat the declined campaign, silently relabel assistant grades as human,
or infer new paid trial permission. Missing billing and human-effort measures
remain unknown. Full plan/build draft remains unapproved and unimplemented.

O-12 logs shared work identity for remaining manual handoffs; O-13 logs reusable
repair-evidence validation with its existing owner. O-10 specialist testing
handoffs remain second priority unless a first-priority dependency is demonstrated.
Other logged form-lifetime/access-separation work retains its prior boundaries.
Patrick asked whether more input was needed: none was needed to finish this
sequence. Bring back a concrete native/design decision before dependent new work.

# Completed: test this change — approved first slice

2026-09-17. Patrick approved the four-item implementation boundary; all three
implementation units are complete. The test verb captures changed inputs,
selects/discloses scope, executes supervised pytest and retains a durable grammar
result through the existing task/recovery mechanisms. Status/resume do not repeat
completed tests. attune-verify0.6.0 remains; no verifier replacement is needed.

Final evidence: 307 source checks, 304 installed checks (3 source-only exclusions),
80 real Spec tests through installed preview/accept/status/resume, 11 matching
module hashes, 4/4 protection removals detected (6/7 tests fail; one control stays
green), independent review findings repaired and centrally checked. The real case
has one dispatch and an unchanged record on reuse. See
`docs/test-this-change-results.md` and `docs/receipts/test-this-change-implementation/`.

New wheel only in a disposable target install; no active plugin upgrade or release.
No AI checkout changed. Qualified scope is local POSIX regular-file Git working-tree
changes and default pytest file discovery. Custom patterns/collectors and legacy
specialist handoffs remain separate qualification work. Tests are trusted code,
not an external-effects sandbox. Plan: `.claude/plans/test-this-change.md`.

O-04's first connected testing journey is complete; O-10 retains unmerged specialist
handoff defects. O-11 logs measured intake/status latency for a later bounded
profiling decision. Prior form-lifetime and access-separation opportunities remain
in their existing states; this task does not authorize starting them.

# Completed work: release-readiness follow-through

2026-09-17. Patrick approved morning recommendations 1–3 and then directed using
available qualified Verify 0.6.0, with logged opportunities pursued after these
three tasks. The separate memory-adoption ladder below remains complete.

Active plan: `.claude/plans/release-readiness-follow-through.md`; canonical
workspace `spec-0307a679054f41ca80eb17785f39354e`. Tasks 1 and 2 accepted at revisions
1 and 3; Task 3 installation and independent support review passed. The active
plugin failed terminal rendering of resumed empty planning probes. The repaired
disposable installed host completed under the existing auto-run authorization;
`.claude/plans/release-readiness-follow-through.md` now saves completed 1–3 with
all three exact accepted executor receipts. See `task3-installed-host-acceptance.json`
and `close_with_installed_host.py` in the follow-through receipts directory. The
old daemon workspace is not claimed terminal; its failed display attempt is
retained. This did not upgrade or activate a plugin.
The bounded closeout review verified durable 3/3 state. Its two provenance limits
were closed by independently checking original session tool-call excerpts and a
read-only installed-origin audit. Final report: `docs/release-readiness-follow-through-results.md`.
Do not reopen old memory acceptance forms or the withdrawn Spec review concern.

- Task 1: `docs/release-journey-map.md`, 149 passing source journey checks under
  Verify 0.6.0; plan/build still scoping-only. The initial Verify 0.6.1 mismatch
  and independent map's one hash transcription correction remain documented.
- Task 2: `docs/spec-completion-repair-results.md`; 153 Spec/host checks,
  678 repository gates, 4/4 protection removals, independent review passed with
  14 hashes matched. Actual accepted evidence survives completion/resume. The
  existing shared state format permits missing historical receipts, visibly
  disclosed; a reviewer concern was withdrawn after checking the legacy runner.
- Task 3: `docs/fresh-installation-results.md`; public dependency resolution in
  two disposable macOS/Python 3.12 environments, 74 combined packages, Verify
  0.6.0. Installed 151 memory + 24 rollback + 80 Spec + 146 journey checks pass;
  three source-only tests remain covered by the earlier source run. 13 module
  hashes match, no borrowed site-packages/source paths, no `.pth`, zero runtime
  socket-guard attempts. Optional Harness removal/restoration is a rollback
  check in the combined environment, not a third fresh resolution.

AI changes remain solely in `/Users/patrickroebuck/attune-ai-memory-adoption`.
The original AI checkout still has only its pre-existing untracked usage snapshot.
No release, active installation upgrade or native/provider campaign occurred.
Both prior automations remain paused. The active installed plugin still uses
its old completion renderer; the repaired candidate is independently installed
and tested in a disposable environment.

First next opportunity completed: the primary review CLI journey passes source
and installed checks (one additional test; no production change). Patrick then
selected the testing consolidation case described above. Retain native
form-lifetime diagnosis; access-separation work remains deferred. Preserve all
useful features and use the existing verb/grammar direction.

# Historical shared memory adoption handoff

2026-09-17. **All five tasks accepted; the approved memory ladder is complete.**
Canonical workspace: `spec-461d7dcb00604997b9ac1110d4df773b`.
Task 4 accepted at revision 21; Task 5 at terminal revision 23. The returned state
was saved through the spec state API and reread: completed 1–5, current null.
The completed-work heartbeat stays PAUSED. The one-time 9:00 a.m. Eastern follow-up
on September 17 reviewed the opportunity log and verified the final records;
`morning-opportunities-review` is now PAUSED. No new implementation was started.

## Outcome and scope

The five-task plan adopts one shared memory core in Harness and the optional AI
consumer while preserving useful existing memories and paths. Tasks are accepted
under Patrick's existing auto-run policy, with a pause on unresolved serious
findings. Live activation, release, migration and new native/provider campaigns
remain outside approval. The user explicitly requested a bounded review; do not
turn its completion into a general identity/security program.

Harness work is in `/Users/patrickroebuck/attune-harness`, branch
`codex/update-session-starter`, base HEAD
`fc65e74fc7c289f71f3a7d081dfe7c1be1238c33`. Preserve unrelated dirty work.
AI changes are isolated in `/Users/patrickroebuck/attune-ai-memory-adoption`, branch
`codex/shared-memory-adoption`, base HEAD
`fe08f282fb0ad9cbb7eedf75af2597336576578e`. The original AI checkout remains
unchanged except for its pre-existing untracked usage-snapshot file.

## Completed evidence

- Tasks 1–3 preserve current formats, extract the dependency-free proposal worker
  and add explicit-root raw/personal/curated readers. Legacy keyed memory and
  persisted-pattern paths remain; new managed legacy-store mutations are unavailable.
- Task 4 exposes both actual CLI hosts and an explicitly activated AI MCP plugin.
  Receiving context supports full-source handles and explicit whole-packet refresh.
  Partial/unavailable retrieval and refresh report their status and exit 2.
- The bounded review found one historical-job authorization defect. The repair
  checks the exact saved job before inspection return, before replay responses
  and before replay return. Twenty integration cases pass; all six new cases fail
  when the new guard is removed. Independent source review closed the finding,
  all 31 inspected hashes matched centrally, and the final focused six-file source
  selection passed 215 tests. Earlier 221-test adapter-repair evidence is separate.
- Existing unchanged AI evidence: 678 quality/gate checks, 87 preflight checks,
  changed dispatch and handler coverage 100%, adapter coverage 91.09%.
  Earlier four protection removals and shutdown upload positive control remain
  recorded. The original broader review refusal stays a historical non-pass;
  the later authorized passive review has its own scope and receipt.
- Final wheels were checked through three temporary environments: Harness alone,
  both products, and AI without Harness. Outside-tree consumers pass 151 memory
  checks plus 24 legacy checks with the additive route disabled. Eleven loaded
  production-module hashes match source. Zero intercepted network attempts.
- Wheel SHA-256: Harness
  `4f349e3dca8bb68e6d02d1acb52a55dc973af18bc640445b8296e1804af0132a`;
  AI `10e18684f521a4ebc56d3280bd408599d8d7fffa6e58c87d789ab1fe140ae91b`.
- Installation scripts and post-install interpreter audits document the plain
  dependency path in both AI environments. Existing installed dependencies were
  reused without editable hooks; fresh resolver installation is not qualified.
  Discarded raw pip stdout is disclosed rather than recreated as a transcript.

## Exact limits

Support evidence is macOS 26.6.2 arm64, Python 3.10.11, AI MCP SDK 1.29.1.
Other platforms, Python versions, fresh dependency resolution and Harness's separate
MCP extra remain unqualified. Worker execution replays supplied proposals; it does
not dispatch Luna/Astra. Context refresh requires caller replacement before the
next turn. It does not erase conversational history or automatically update all
agents. Config/journals are trusted local host files; general multi-user identity,
hostile filesystem ownership and dynamic role revocation are deferred.

Actor labels in fixtures are synthetic. Patrick is the sole developer; email
aliases do not create multiple people. No personal identity mapping was added.
All integration fixtures are disposable. Separately, an explicitly discussed
collaboration reflection was retained in personal global memory, not the product.

The final telemetry decision preserves local accounting and explicitly configured
team operations while excluding new-route usage uploads through shutdown. The
older entirely-offline choice was superseded. Product-wide uploader retirement
and live Redis collaboration are separate work.

## Evidence and continuation

- [Support matrix](../specs/shared-memory-adoption/verification.md)
- [Task 4 acceptance](../receipts/shared-memory-adoption-task4/acceptance.json)
- [Bounded review and repair closure](../receipts/shared-memory-adoption-task4/bounded-review/central-closure.json)
- [Task 5 qualification](../receipts/shared-memory-adoption-task5/qualification.json)
- [Opportunities for later steering](../opportunity-log.md)
- [Historical pre-review handoff](archive/shared-memory-before-bounded-review.md)

The final support reviewer found one provenance gap, closed with a construction
receipt and actual audits of both AI environments. No review findings remain;
all 19 reviewed input hashes matched centrally. R5 ledger and its two format checks
are complete. Task 5 acceptance and current qualification receipts are saved.

One observed spec display defect remains outside this completed plan: the terminal
screen repeats old planning probes, even while its result and saved state correctly
show all five tasks completed. O-08 logs it; do not treat its old `completed=[]`
or “No implementation” text as current. Preserve the raw receipt.

Morning steering recommendation: map release-critical installed journeys (O-04)
to identify actual Harness blockers; O-08 is the smallest confirmed shared repair,
and O-07 is the next installation-qualification option. Keep useful features and
the form-lifetime issue visible. Opportunities are choices, not an automatically
approved implementation queue. No approval or live activation is implied.
Do not replay the historical revision 19 gate.
