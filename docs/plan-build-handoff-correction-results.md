# Connected handoff correction — September 18, 2026

**The local corrections are implemented and qualified: 267 distinct passing
source checks, 267 isolated-wheel checks, and 4/4 runner guard removals detected.**
Local qualification made no native calls. Task 8 still needs fresh connected
model evidence.

The protected fixture runner selects checkout/src, checks the actual package,
documentation and exporter origins, and rejects empty discovery or failed tests.
Its bytes are frozen as protected input. Ordinary generated tests no longer need
to import the acceptance harness to make imports work. The product runner's clean
environment restrictions remain enforced.

The v2 planner explicitly requires every response field and `choices: []` when
there are no new material choices. Strict validation is unchanged: omitted, null,
malformed and participant-selected choices fail. Empty new choices preserve an
existing human decision and do not grant acceptance. This prompt clarification
does not yet demonstrate improved native compliance; the original malformed Luna
reply still fails unchanged.

Source and installed replays reuse five original Astra replies with identical
payloads and a clearly scripted final reviewer. Both complete the actual Spec,
effect and test route, reject a changed protected runner before dispatch, preserve
the completed exporter through synthetic correction/reacceptance, finish without
repeated calls and reject a later source change. Original generated tests and
protected checks execute. These are local replays, not fresh native journeys.
The scripted correction remains substantively redundant with the original plan;
novel changed-intent adaptation is not established.

Four disposable mutations independently remove source selection, origin checking,
empty-discovery rejection and failed-test propagation. Each corresponding test
passes before removal and fails afterward. Installed-shadow, wrong-origin,
missing-export and malformed-choice negatives remain in the regular suites.

The first replay check expected ValueError, while the existing freshness owner
correctly raised UnresolvedOperation. The test harness was corrected to recognize
that specific refusal; product enforcement was not weakened. The first offline
wheel build lacked setuptools in the test environment. The existing Attune AI
build environment produced a wheel without downloading packages; that wheel was
tested in the isolated Python 3.12 environment. Both initial failures are retained.
The optional Spec dependency emits its existing ModelTier deprecation.

The experimental meter binds the selected prepared packet and records its actual
budget. Closed allocations refuse dispatch. Injected native-envelope tests
exercise the six-call ceiling, model counts, subscription routing, API-key
removal and changed-packet rejection; those tests do not call providers.

Patrick subsequently approved the proposed **40-credit planning limit** for one
fresh six-call journey: Luna four, Astra two, expected 15–30 estimated credits,
existing ChatGPT and zero new API dollars. That allocation is separate from the
closed comparison and this local qualification. It does not accept Task 8 or
release the product. No provider-backed quality pipeline or independent semantic
review ran for this local correction.

Evidence: [source suite](receipts/plan-build-handoff-correction-2026-09-18/source-tests.xml),
[additional checks](receipts/plan-build-handoff-correction-2026-09-18/final-experiment-tests.xml),
[installed suite](receipts/plan-build-handoff-correction-2026-09-18/installed-tests.xml),
[installed additional checks](receipts/plan-build-handoff-correction-2026-09-18/installed-final-experiment-tests.xml),
[source replay](receipts/plan-build-handoff-correction-2026-09-18/source-final-replay/qualification.json),
[installed replay](receipts/plan-build-handoff-correction-2026-09-18/installed-final-replay/qualification.json),
[guard trials](receipts/plan-build-handoff-correction-2026-09-18/guard-removal.json).
