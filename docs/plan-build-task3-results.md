# Plan/build Task 3 — planning assignments and conversation

Tasks 1 and 2 are accepted. Task 3 is implemented and locally qualified; its
human acceptance is pending. Tasks 4–8 have not begun. Auto-run remains false.

Draft work now reaches configured planner and critic assignments through the
existing transport, process supervision, journal and task store. Known missing
intent uses existing forms without a participant call. Partial answers, unresolved
choices and corrections remain attached to the same work identity. Complete
requests avoid another intake; alternatives show their counter-cases and unknowns.

Native planning has an explicit operation profile: planner maps to lead, critic
to reviewer, while the declared role remains recorded. The host binds protocol
identities around substantive replies. Legacy review behavior retains its existing
profile. Native mapping was tested with injected transport; no native call ran.

Replies are proposals. They cannot replace the user goal, omit named acceptance
criteria, expand file scope, choose an unresolved alternative or claim approval.
Critique findings retain evidence separately from optional advice. High findings
stop staging. Staging produces another unaccepted draft, preserving the prior
conversation. It refuses to refresh changed inputs under an old proposal.

## Evidence

| Actual check | Result |
| --- | --- |
| [Source suite](receipts/plan-build-task3-2026-09-18/final-source-tests.xml) | 431 passed, including 42 planning cases, 84 contract cases and existing review/native-adapter/task/repair/recovery/test routes. No skipped or xfailed tests. One inherited Attune AI deprecation warning. |
| [Installed wheel suite](receipts/plan-build-task3-2026-09-18/installed-tests.xml) | 430 passed outside the checkout; excludes only the source-preparation experiment. Actual package built and installed into an isolated target with no index or dependency resolution. |
| [Installed identity](receipts/plan-build-task3-2026-09-18/installed-identity.json) | Four changed runtime module hashes match the source; active installation untouched. [Isolated base import](receipts/plan-build-task3-2026-09-18/base-import.log) needs no optional Attune packages. |
| [Guard removal](receipts/plan-build-task3-2026-09-18/guard-removal.json) | All five independently removed protections detected: goal retention, acceptance-criterion coverage, required planning control, native permission and staging freshness. Baseline negatives pass. |
| [Coverage](receipts/plan-build-task3-2026-09-18/changed-line-coverage.json) | New runtime: 254/279 lines, 91.04%. Changed contract lines: 88%; participant lines: 86.36%; routing lines: 100%. Line coverage is not semantic outcome accuracy. |
| [Lifecycle](receipts/plan-build-task3-2026-09-18/lifecycle.json) | Symbol reality and falsifiability PASS; 31 cited tokens checked; no waiver. |
| [Retained journey](receipts/plan-build-task3-2026-09-18/journey-result.json) | Actual forms, four local command subprocess calls, original/corrected intent, five draft revisions and attributed planner/critic results. Synthetic host decision projection; no real collector bridge claim. |

The retained journey first asks only for the missing goal and makes zero
participant calls. Its explicitly synthetic original request is corrected to
retain unknown/refuted findings. The final record preserves that correction,
the default-output requirement and unrelated dirty content. No proposed feature
file is written and no effect grant is created.

Observed backend readiness was 1.88 ms for known missing information, 33.14 ms
for the original local proposal, and 34.57 ms for the corrected proposal; the
corrected two-participant call completed in 67.25 ms. These are single synthetic
local observations, not desktop form latency, native model latency or human
response time. Repeated completed calls reuse the receipt without redispatch.

Real local subprocess tests cover nonzero exit, missing executable, timeout and
malformed output. Lost acknowledgment leaves an uncertain journal entry; blind
retry and correction are blocked. Missing identities, insufficient budgets,
unsupported required hooks, stale source, invalid scope/dependencies, unnecessary
intake and incorrect responses fail visibly before producing an accepted plan.

During qualification, the first deterministic read-only operation was incorrectly
rejected by the new journal validator. The validator now distinguishes the
existing deterministic effect classification from conservative unknown external
effects. Two injected-native fixture assertions also used the wrong Task field;
they were corrected against the actual `requirements` contract. Original failed
results remain in the receipt directory. Legacy role mapping was kept specific
to its operation profile during review, and the staging freshness gap received
a dedicated negative test and protection-removal check.

Black and Ruff pass on the new runtime, changed work contract and new tests.
This was local implementation review and qualification; no independent model
review or provider-backed quality pipeline ran. Any Spec score denotes checklist
completion only. Both preceding receipt manifests remain unchanged.

## Qualified boundary and next task

The parser checks explicit intent retention and declared criterion coverage; it
cannot prove that arbitrary task prose actually fulfills the user's goal. Native
planning competence, response quality and cost remain Task 8 outcome work.
No additional paid/native allocation was used.

Planning uses trusted configured commands with existing supervision, which is not
a general security sandbox. Source freshness is checked around calls and at
staging, but source files are not locked by the task-store lease. Required planning
hooks without a qualified runner stop before participant dispatch. General hook
and effect enforcement are Task 4 work.

The Spec decision fixture only tests the host projection into the shared record.
The actual collector bridge remains Task 6. Recovery refuses uncertain attempts;
the full reconciliation/intervention experience remains later work. External
planning is currently an explicit Python API operation; installed plan/build
CLI registration remains Task 7. No activation or release occurred.

Recommended next step: accept Task 3 and proceed to Task 4's bounded file creation,
modification, required controls and crash/reconciliation qualification. The strongest
counter-case is that native outcome quality and the live host bridge remain
unqualified; accepting Task 3 covers this disclosed local software boundary.
