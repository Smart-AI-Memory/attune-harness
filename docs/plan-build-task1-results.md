# Plan/build Task 1 — concrete journey and baseline ready

Prepared across 2026-09-17/18 after the authorized Spec handoff repair and closeout.
The existing eight-task draft is now a concrete
[XML execution ladder](../.claude/plans/plan-build.md). Patrick's instruction to
proceed was transcribed through its existing Spec workspace; Task 1 acceptance
is pending. No production plan/build engine changes or additional native calls
have been made.

The selected feature adds JSON Lines export to a byte-identical captured copy of
Harness's existing documentation module. Default JSON output, source hashes,
scope, uncertain/refuted claims and advisory notes must survive. Building it must
create a module and optional generated-test directory, modify the existing CLI,
pass a protected independent oracle, and preserve unrelated dirty work. This is
a small existing-code subtree fixture, not qualification of arbitrary repositories.

## Task 1 evidence

| Evidence | Result and limit |
| --- | --- |
| Existing source baseline | Two actual checks pass: default JSON output/static scope and preservation of unknown claims |
| Frozen future-feature oracle | Two assertions fail and one missing-module error occurs as expected; the unchanged-default control passes. The feature has not been implemented |
| Existing authority/scope/recovery suite plus new preparation test | **32 passed**, no skips/xfails; existing workflow import emits one deprecation warning |
| Envelope probe | RunStore can retain the candidate record; the current task reader correctly rejects its unsupported profile |
| Participant probe | Planner/critic roles are currently rejected; worker/reviewer still use the review protocol. Transport construction was replaced with a recording stub; no model inference occurred |
| Legacy import probe | Supported paths/dependencies survive; unknown XML is silently ignored and non-task input returns an empty list. The future bridge must disclose/reject these cases |
| Creation/recovery scratch probe | Exclusive create preserves complete bytes after lost acknowledgment; blind replay and collision are rejected; an observed completion is reused without another write. This uses a trusted existing parent and is not production containment qualification |
| Existing lifecycle checks | Tasks and execution boundaries PASS; each symbol check covers **20 cited existing paths**. Future modules are explicitly marked as planned, not claimed to exist |

The [first-journey design](specs/plan-build/first-journey.md) fixes exact module
boundaries and names each control's enforcer. The eight steps remain: baseline,
shared contract, planning assignments, effects, dependent build, steering/Spec
controls, installed experience, and outcome qualification. Task 8 prepares its
native comparison and stops before dispatch unless separately authorized.

## Decision — persistent fallback

**A — Accept Task 1 and continue (Recommended).** Freeze this baseline and implement
Task 2's shared work contract, retaining later quality gates.

**B — Accept and auto-run remaining local tasks.** Use the existing auto-run rule;
pause on serious findings or the separately controlled paid-trial decision.

**C — Revise Task 1.** Adjust the concrete fixture, scope or baseline before the
shared contract is implemented.

Recommendation rationale: actual probes now identify the needed changes, and the
independent acceptance oracle is frozen before the worker can propose effects.
The strongest counter-case is that this small subtree cannot establish large-repo,
cross-host or native-model reliability; the later ladder must supply that evidence.
The preparation score is checklist completion, not a product/model quality grade.

The [canonical Spec form](receipts/plan-build-task1-2026-09-17/task1-gate.md) and
[workspace audit](receipts/plan-build-task1-2026-09-17/workspace-audit.json) retain
the exact action binding. Before collecting a reply, verify the captured plan and
evidence identities. The currently active MCP adapter is version 1; this task
does not claim it has been replaced by the newly qualified candidate handoff.
Its generic planning receipt is not a Harness test-evidence binding.

## Retained artifacts

- [Preparation result](receipts/plan-build-task1-2026-09-17/qualified/result.json)
- [32-check suite](receipts/plan-build-task1-2026-09-17/baseline-tests.xml)
- [Lifecycle receipts](receipts/plan-build-task1-2026-09-17/lifecycle.json)
- [Parsed eight-task ladder](receipts/plan-build-task1-2026-09-17/parsed-plan.json)
- [Current unaccepted task state](receipts/plan-build-task1-2026-09-17/execution-state.json)

Earlier preparation directories are retained as development evidence. The initial
fixture commit encountered local signing configuration; the final isolated fixture
disables signing and hooks only for its synthetic commit. The first lifecycle run
checked zero path tokens because the outline contained links/table prose; the final
document explicitly cites the 20 existing artifacts so the gate performs a real
source check. Neither initial result is substituted for final qualification.

Reflection: reuse requires testing the consumer boundary. Persistence alone does
not make a new task executable, a role name does not create a planning protocol,
and parsed XML does not establish lossless import. These observations narrow the
required implementation without removing useful capabilities or adding another
scheduler, renderer or approval store.
