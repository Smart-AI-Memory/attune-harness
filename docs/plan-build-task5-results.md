# Plan/build Task 5 — dependent feature build

Tasks 1–5 are accepted. The actual Spec workspace accepted Task 5 automatically
under Patrick’s non-high continuation choice (revision 20). Paid/native trials
remain separate.

Build now executes accepted dependent tasks through configured worker/reviewer
assignments, existing transport and the same effect journal. Each step receives
earlier outputs and only its accepted output scope. Protected verification runs
between steps and on the final artifact. A failing oracle stops continuation;
generated-test success or model opinion cannot replace that evidence. Completion
can hand off to the existing pytest owner with current source/revision bindings.

## Evidence

| Actual check | Result |
| --- | --- |
| Source tests | [715 pass](receipts/plan-build-task5-2026-09-18/full-source-tests.xml), plus [two focused boundary cases](receipts/plan-build-task5-2026-09-18/final-boundary-tests.xml): **717 total**, including 51 dependent-build cases. No skips or xfails; one inherited Attune AI deprecation warning. |
| Installed wheel | [715 pass](receipts/plan-build-task5-2026-09-18/installed-tests.xml), plus [the final owner-boundary case](receipts/plan-build-task5-2026-09-18/installed-boundary-test.xml): **716 total**, excluding only source preparation. All eight changed module hashes match source; base import needs no optional Attune packages. |
| Protection removal | [7/7 detected](receipts/plan-build-task5-2026-09-18/guard-removal.json): dependency, fixed verification, step output scope, failed oracle, native permission, stale handoff and contradictory check outcome. Baseline negatives pass. |
| Coverage | [New policy 90.94%; effect changes 92.68%; other changed source files 90.91–100%](receipts/plan-build-task5-2026-09-18/changed-line-coverage.json). Software line coverage, not model quality. |
| Lifecycle and preservation | Both lifecycle checks pass; 50 cited tokens. All 182 prior Task 1–4 receipt files remain unchanged. |
| Selected journey | [Captured documentation module](receipts/plan-build-task5-2026-09-18/journey-result.json), three dependent steps, four actual local command calls, four passing protected probes, new module/generated-test directory, actual pytest handoff, preserved dirty work and stale-source rejection. Production documentation is unchanged. |

The real fixture preserves default JSON, unknown/refuted findings and advisory
notes while adding JSON Lines output. It uses fixed scripted proposals and a
scripted reviewer. The first useful worker result arrived in **198 ms**; the build
completed in **1.24 s** of wall time. These single backend measurements exclude
native inference, desktop rendering and human response time.

A separate wrong implementation returns 43 instead of the required 42. Its
generated `assert True` test passes through the actual existing pytest route, but
the protected oracle fails and the build remains `needs_revision`. Thirteen saved
operation boundaries resume without repeated participant calls or file writes.
Uncertain participant/check responses remain blocked; file effects use explicit
reconciliation. Rechecksummed but inconsistent receipts are rejected.

## Corrections and limits

The first test fixture lacked a Git HEAD required by existing test intake; it now
creates its disposable baseline before freezing effects. A later test expected a
resume-authority error where the earlier external-permission guard correctly
refused dispatch; the test now expects that established guard.

The first outcome-validator mutation survived because a later correlated-turn
hash also detected the changed receipt. Its direct negative case now isolates the
outcome check, while separate whole-journal negatives retain integration coverage.
Original failures and mutation logs are retained. The initial auto-run projection
also needed the collector's explicit confirmation field; the user's same B choice
was then accepted, without requesting another decision.

This profile supports an ordered task chain with one producer per output in a
dedicated local POSIX checkout. Checks are trusted, fixed bounded commands, not a
security sandbox. Full graph scheduling, repeated output edits, revision after
effects, broader hook/intervention handling and the actual Spec bridge remain
unqualified. No independent model review, provider-backed quality pipeline,
native/provider call, paid trial, live activation or release ran.

The next authorized task is Task 6: connect the existing Spec collector, human
steering and supported legacy plans while preserving one current work record.
The strongest counter-case remains the unqualified live/native boundary; the
successful local journey does not establish native model effectiveness.
