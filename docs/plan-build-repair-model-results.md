# Repair-worker comparison — September 18, 2026

**All 18 original repairs pass. Keep Luna as the provisional worker for the next
connected repair trial.** Each model passes both repetitions of all three cases;
Luna has the lowest observed cost. This meets the frozen screen's candidate floor,
not Task 8's complete-journey acceptance boundary. No production routing changed.

Patrick approved the prepared 18-call comparison with “a/approve.” The allocation
is now closed: **79.574004 estimated new Codex credits** against its separate
150-credit planning ceiling, **$0 new API dollars**, no retries or unused slots.
These are token-based estimates, not verified account deductions. Earlier trial
allocations and their original failures remain separate and unchanged.

| Requested model | Original repairs | Estimated credits, six calls | Credits per successful repair | Median native response time |
|---|---:|---:|---:|---:|
| Luna 5.6 | 6/6 | 1.123814 | 0.187302 | 37.1 s |
| Sol 5.6 | 6/6 | 22.555440 | 3.759240 | 32.1 s |
| Astra 6 | 6/6 | 55.894750 | 9.315792 | 44.4 s |

Sol has the lowest median native time in this sample. Luna has the lowest total
native time (272.1 s versus Sol's 276.1 s and Astra's 324.3 s). Neither aggregate
establishes a dependable speed ranking from these few cases. With all three
eligible, the predeclared tie policy favors Luna's lower cost provisionally.

## Cases and actual execution

| Case | Guidance supplied | Luna | Sol | Astra |
|---|---|---:|---:|---:|
| Retained nested-status assertion | Actual KeyError and already diagnosed nested access | 2/2 | 2/2 | 2/2 |
| Fresh injected exporter filtering | Failure symptoms and preservation requirements; no reference patch | 2/2 | 2/2 | 2/2 |
| Fresh injected CLI default regression | Failure symptoms and captured compatibility requirement; no reference patch | 2/2 | 2/2 | 2/2 |

The fresh defects were deliberately injected into disposable copies of previously
checked code. They are related synthetic cases, not newly discovered production
bugs. The retained case is a guided repair, not independent diagnosis evidence.

Every original response passes the current strict worker contract, followed by
inspection before execution. The existing installed Spec collector, build owner,
effect journal and test handoffs apply each scoped replacement in a fresh owner.
Every repair passes **four supplemental and four protected tests**, preserves
their required counts and assertions, retains protected/unrelated files, resumes
without repeating a participant call, and rejects completion after a source edit.
These are repeated executions of the same small test sets, not 144 distinct tests.

Two replies differ from the local reference without introducing a defect:

- Sol's first CLI reply (`rm05`) fixes the default and also makes equivalent
  quoting, Unicode-literal and escape-expression rewrites. Actual compatibility
  and all other checks pass. The extra edits are optional maintainability advice,
  not a fabricated functional failure or an unmet requirement.
- Luna's second-dispatched assertion repair (`rm15`, repetition 1) retains the
  existing iteration-variable name. Its nested access is correct. It passes on
  behavior and preservation, not exact equality with a reference patch.

Grades were frozen before aggregate recommendation. Semantic inspection was by
the session assistant: it is neither independent nor strictly blinded. The final
reviewer in each local replay is scripted; **no native final reviewer ran**.

## Timing and spend evidence

| Case | Luna native times | Sol native times | Astra native times |
|---|---|---|---|
| Retained assertion | 36.8, 37.5 s | 32.3, 32.0 s | 45.5, 43.3 s |
| Injected filtering | 18.8, 18.6 s | 17.8, 17.4 s | 18.6, 19.3 s |
| Injected default | 92.6, 67.9 s | 83.9, 92.7 s | 98.3, 99.4 s |

Times within a cell follow actual dispatch order. Native time includes the local
client and provider round trip. The first fully validated response arrives
17.4–99.4 seconds after dispatch; the first test-backed disposition takes
26.3–113.6 seconds across trials. Local evaluation itself averages 1.25–1.28
seconds by model. Dispatch-to-disposition includes assistant inspection, tool
latency and scheduling; `rm12` includes about 70.5 seconds after the native
response during a session-context transition. Do not attribute that delay to Sol.
No first-token, desktop-rendering or human-attention timing was captured.

Full-file source replies range from 631 to 9,685 bytes. The longest replies are
the CLI repairs, but this observation cannot isolate response size as the cause
of latency. Opportunity O-34 retains a possible targeted-edit experiment for
after the current journey, with the same host binding and safety checks.

| Requested model | Input tokens | Cached input subset | Output tokens |
|---|---:|---:|---:|
| Luna | 164,848 | 11,008 | 11,637 |
| Sol | 174,236 | 10,624 | 12,176 |
| Astra | 179,404 | 0 | 8,835 |

The existing meter applies the Standard rates frozen in the approved preparation.
All 18 calls return usable usage; cache savings are included. The earlier
unresolved startup allowance is not a measured charge in this campaign.

## Qualification limits and next boundary

Frozen case turns, requested high reasoning and Standard service were the same
for each model's repetitions. Native processes used ChatGPT authentication and
an inner read-only sandbox. No tool-execution item appears in the retained native
streams. Each stream contains a nonfatal skills-context-budget warning; the
omitted-skill count varies between 165 and 167. Host startup context was therefore
not completely isolated, and total input-token counts differ. This is a comparison
of the configured Harness profile, not proof of identical complete model inputs.
The streams do not report a backend model name; identities above are the requested
models recorded in the actual dispatch arguments.

The result supports keeping Luna for these bounded repairs. It does not prove
why the earlier unchanged reply occurred, erase that failure, establish general
repair reliability, qualify memory sorting, or complete native review and handoff.
Tasks 1–7 stay accepted; **Task 8 remains open**. All **12,879 earlier receipt
files** captured at admission retain their hashes. Existing memory stores and
unrelated work were not modified.

Next, prepare a connected continuation using Luna for the retained guided repair
and Astra for native final review, with actual retesting, preserved completed work
and stale-decision rejection. Reuse the established Spec acceptance gate. The
old two-call packet remains superseded; any new paid execution needs its own
concrete call/cost decision. Do not spend the unused portion of this closed
150-credit ceiling as an implicit new allocation.

Evidence: [frozen protocol](receipts/plan-build-repair-model-preparation-2026-09-18/protocol.json),
[authorization](receipts/plan-build-repair-model-native-2026-09-18/authorization.json),
[grades](receipts/plan-build-repair-model-native-2026-09-18/grades.json),
[grade freeze](receipts/plan-build-repair-model-native-2026-09-18/grade-freeze.json),
[summary](receipts/plan-build-repair-model-native-2026-09-18/summary.json),
[timing and sizes](receipts/plan-build-repair-model-native-2026-09-18/timing-and-size.json),
[transport audit](receipts/plan-build-repair-model-native-2026-09-18/transport-audit.json),
[closed ledger](receipts/plan-build-repair-model-native-2026-09-18/ledger.json).
