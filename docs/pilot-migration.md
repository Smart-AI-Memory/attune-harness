# Pilot migration decision record

Owner: Patrick Roebuck. Selected workflow: evidence/documentation review in
attune-harness. This record separates technical qualification from a personal
choice to make the workflow the preferred daily path.

| Concern | Current path | Opt-in replacement / preservation |
|---|---|---|
| Review request | Patrick asks Codex to inspect documentation and experiment evidence; scripts produce local receipts. | Accepted Attune form records objective, document, corpus, trusted context and distinct participants. |
| Evidence | Existing E1/E2 scripts and audit JSON remain the primary experimental evidence. | Keyword retrieval supplies source paths, hashes and excerpts. The review links to evidence; it does not rerun or independently certify experiments. |
| Verification | Existing strict attune-verify integration and manual checks. | The same pinned library owns extracted-claim verified/refuted/unknown outcomes. Model prose is kept separate. |
| Review narratives | Current assistant reviews and explains the evidence. | Two explicitly selected local model roles receive independent tool histories; outputs are proposals for inspection. One quantized model is not native multi-provider qualification. |
| State and recovery | Original scripts/receipts remain available; prior Harness dev0 environments are retained. | New local run directories persist accepted inputs, events, evidence and raw generations. Completed events replay; uncertain effects block. |
| Data/configuration | Existing repo documents, contexts, baseline wheels and attune-ai settings. | Reference originals in place. Generate isolated request/configuration files; no conversion, deletion or shared-state dual writer. |
| Rollback | Use the retained old environment and artifact. | Restore the old wheel in the opt-in environment, inspect the new completed record, and run the earlier supported deterministic workflow. The local-model peer was introduced in dev3; configurable output budgets require dev4; the optional qualified tokenizer requires dev5. |
| Extensions | Explicit data-only bundle and scoped retrieval binding. | Retained original bundle enables upgrade rollback. Rejected upgrades preserve disabled state; removal retains user data and blocks new dispatch. |

Technical acceptance is governed by [the pilot journey](pilot-workflow.md) and
its retained installation, regression, live review and rollback receipts. The
local model's arithmetic calibration failed, so neither successful generation
nor agreement between its roles establishes model reliability. Do not switch
adaptive collaboration on by default or automate disposition of findings from
this pilot.

M1 is an assisted trial: Codex carries out Patrick's selected real workflow under
his authorization. Patrick has not been recorded as personally operating the
CLI. M2 (his preferred path) remains an owner decision after examining the
concrete result. Human repair time and comparative usability are unmeasured.
M3 moves later workflows individually with their required capabilities and tests.
M4 reviews remaining attune-ai responsibilities; retirement is not authorized.

Keep attune-ai's existing public interfaces and maintenance path. Put new portable
Harness capabilities here while evaluating this opt-in workflow. No blanket
feature freeze, migration deadline, public release or runtime replacement follows
from these local receipts.
