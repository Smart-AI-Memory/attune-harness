# Connected plan/build comparison — September 18, 2026

**The approved comparison stopped with two distinct handoff failures. All four
review controls passed, but neither original native journey completed. Task 8
remains open.** Ten native calls used **66.959635 estimated Codex credits**;
six reserved calls were left unused, with no retry, replacement or fallback.
No new API dollars, live memory operations, active installation changes or
release were involved.

## Original outcomes

| Case | Result | Meaning |
|---|---|---|
| Four Astra review controls | 4/4 pass | Accepts the sound plan and exporter; identifies the contradictory plan and the known CLI class-identity defect, with evidence. |
| Luna routine journey | Stops after planner | Goal and scope are preserved, but the original response omits required `choices`. No critic or build runs. |
| Astra reference journey | Stops at supplemental-test discovery | Plan, critique, exporter and CLI pass their boundaries. The frozen runner cannot import the newly produced exporter. No final reviewer or completion handoff runs. |

Nine of ten original responses satisfy their role contracts. Luna's response is
not silently repaired by inserting an empty field. This is a response-contract
failure, not a demonstrated misunderstanding of the requested feature. Its five
remaining calls are unused. The Astra arm uses five calls; its final reviewer
call is unused. Both original outcomes remain failures against the complete
journey floor.

The review controls distinguish contradictions and unsupported guarantees from
legitimate uncertainty. Both defective cases receive specific high-severity
findings; both correct cases retain empty findings and qualified notes. The
reviewer does not invent test execution or treat unmeasured performance as a
defect. These four cases support the bounded review behavior only, not general
review reliability or independence between same-model roles.

## Test-runner defect and unchanged-source diagnosis

The supplemental probe invokes standard-library unittest discovery with a clean
environment that does not expose the disposable checkout's `src` directory. The
native plan explicitly says the command needs `src` on the import path. Its
generated tests use ordinary package imports. Discovery instead reaches the
installed package and fails to find the new exporter. The host correctly records
the failed check and stops with `needs_revision`.

The local preparation masked this defect: its scripted supplemental tests import
the protected acceptance module, which inserts the fixture source path. The
runner thus depended on setup hidden inside that particular test implementation.
The pre-execution source inspection also missed this runner/test mismatch. This
is an experimental handoff defect; making every generated test repair its own
execution environment is not the preferred correction.

A separately retained local diagnostic supplies the fixture source path without
changing any original source, response, journal or frozen probe. **All six native
supplemental tests and all four protected feature checks pass.** Imported module
paths confirm the fixture source was exercised. The tests assert complete receipt
reconstruction, mixed statuses, duplicate order, escaping, advisory notes, empty
output and default/explicit CLI compatibility. This diagnostic supports artifact
correctness; it does not turn the stopped original journey into a passing one.

Before the import failure, the original route passed two baseline executions,
the exporter probe and all four protected CLI checks. Complete source proposals
were inspected before file effects or test execution. Effects stayed within the
three approved outputs and the approved missing parent directory. Protected
inputs and unrelated dirty fixture work are preserved.

## Controls exercised and boundaries still open

The Astra arm uses the actual installed work/effect owners and optional Spec
collector. After the first verified output, a synthetic pending-task correction
invalidates prior acceptance. The collector reaccepts the revised work; replayed
decisions are rejected. The completed exporter remains byte-identical and is not
rebuilt. These are synthetic fixture actions, not an actual human interaction.

The appended reconstruction assertion already appears substantively in Astra's
plan. This qualifies revision binding and preservation through the collector,
but is weaker evidence for adapting to a novel change in intent. Final native
review, completed-journey resumption and source-change invalidation were **not
reached in either original journey**. Earlier scripted proofs are retained as
local evidence and do not fill those native gaps.

No critical enforcement miss was observed in the exercised operations. That does
not establish enforcement coverage for the stages that never ran. Product source
and earlier frozen receipts remain separate from this disposable comparison.

## Cost and timing

Patrick's “do A” approved sixteen calls maximum: Luna four and Astra twelve,
80–130 estimated credits with a 160-credit planning ceiling, existing ChatGPT,
and zero new API dollars. The allocation is closed with six unused calls; it is
not an allowance for replacement responses.

| Work | Calls | Estimated credits | Dispatch through returned-payload validation |
|---|---:|---:|---|
| Four Astra controls | 4 | 27.870250 | 15.99–22.90 s each |
| Luna planner | 1 | 0.145685 | 26.40 s; fails subsequent role validation |
| Astra planner and critic | 2 | 14.916250 | 43.43 s and 15.30 s |
| Astra exporter, CLI and test workers | 3 | 24.027450 | 20.58 s, 94.23 s and 78.36 s |

Credits are token-based estimates, not verified account deductions. The first
specific, subsequently upheld control finding returned in **19.67 seconds**.
The durable grade for all four controls was saved **298.46 seconds** after the
first request; this includes assistant preparation/inspection gaps, not just
model time. The Astra plan returned in 43.43 seconds and was subsequently judged
useful and faithful. Transport timing alone does not prove useful meaning.

Astra's five validated replies total 251.90 seconds. Fixture creation to stopped
result is approximately 498.36 seconds, based on retained file timestamps and
including orchestration, inspection, synthetic steering and local diagnosis.
Luna's corresponding elapsed interval is approximately 72.37 seconds. These are
incomplete, unequal journeys and cannot support a model latency ranking. Runtime
metrics are retained by revision, since steering starts a new build record.
Desktop presentation and actual human-response timing remain unknown.

All ten invocations retain skills-budget errors and startup diagnostics. Each
reports one completed turn; no tool-use item is observed. Backend model names are
not reported, so attribution is to requested model and argv. Native startup
context remains uncontrolled. The session assistant graded labeled original
responses and source: no blinding or independent semantic grading is claimed.

All 996 files in earlier plan/build receipt manifests, including the prepared
comparison, remain unchanged. The execution lifecycle's symbol and falsifiability
checks pass. Its conservative basename rule also flags the archived fixture
package initializers for chair review. That original receipt is retained: the
files are unchanged evidence copies covered by the approved disposable trial,
not changes to an installed package. No waiver or Task 8 acceptance is inferred.

## Next bounded correction

Repair the host-owned supplemental-test import setup and qualify it with ordinary
generated tests that do not import the acceptance harness. Add a negative that
would detect accidentally testing an installed package instead of changed source.
Keep the runner's environment restrictions and frozen oracle checks intact.

Separately, make the planner's empty-choice grammar explicit: all required keys
remain present, and `choices: []` explicitly represents no unresolved choices. Check
that a malformed or omitted choice cannot acquire authority through silent
normalization. Preserve Luna as a candidate; this one failure does not justify a
stronger router. Further native evidence needs a new concrete allocation after
these local corrections, not a retry hidden inside this comparison.

Evidence: [summary](receipts/plan-build-connected-native-2026-09-18/summary.json),
[original call ledger](receipts/plan-build-connected-native-2026-09-18/ledger.json),
[control grades](receipts/plan-build-connected-native-2026-09-18/control-grades.json),
[original probe outputs](receipts/plan-build-connected-native-2026-09-18/protected-and-generated-probes.json),
[unchanged-source diagnostic](receipts/plan-build-connected-native-2026-09-18/unchanged-source-import-diagnostic.json),
[binding verification](receipts/plan-build-connected-native-2026-09-18/final-binding-verification.json).
