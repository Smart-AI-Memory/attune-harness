# Fresh connected confirmation — September 18, 2026

**The original journey stopped on a real default-output regression after four
calls and 7.744460 estimated Codex credits.** The approved ceiling was six calls
and 40 estimated credits. Two calls remain unused; the allocation is closed.
No retry, replacement, fallback or new API dollars were used. Task 8 is open.

## Original outcome

Luna's planner now includes the required empty choices list and retains the goal,
corrected evidence-preservation requirement, ordered scope and dependencies.
Astra's critique correctly notes that the plan omits the exact finding-record
envelope. That medium finding is nonblocking in this fixture because the protected
oracle explicitly supplies the envelope to the worker. Its wording note correctly
replaces concealment of unmeasured performance with explicit uncertainty. The
session assistant upheld both qualifications; the plan is not graded clean.
Neither original reply was rewritten.

Luna's exporter produces the exact envelope and passes its protected check. The
actual installed Spec collector then invalidates acceptance for a synthetic
pending-task correction, reaccepts the current revision and rejects replay.
The completed exporter remains byte-identical and is not regenerated.

The CLI worker adds the requested opt-in format but also changes an em dash to
an en dash in the existing markdown() output. This changes the default JSON
bundle's markdown value, violating the explicit unchanged-default requirement.
Source inspection catches the difference. An additional local subprocess
comparison captures original output before applying the proposal and compares
it with the unchanged native candidate afterward: receipts match, Markdown and
stdout do not. The original source and proposal are retained.

All four frozen CLI checks nevertheless pass. Their baseline compares the new
CLI with the new Documentation.markdown() result, so both share the regression.
That is an oracle blind spot, not permission to change output. The semantic
assessment stops the trial before the supplemental worker and final reviewer.
The work journal accurately remains paused at its operation boundary; the
experiment's closed result records the semantic failure. No completed handoff,
native final review or stale-completion rejection is claimed for this run.

Four original replies satisfy their role contracts. Four protected probe runs
pass nine test executions including repeats. Those counts do not override the
confirmed compatibility failure. Unrelated dirty fixture work and the completed
exporter are preserved. The four earlier passed review controls are reused
evidence, not freshly dispatched controls.

## Cost and timing

| Role | Requested model | Estimated credits | Dispatch to validated payload |
|---|---|---:|---:|
| Planner | Luna | 0.155080 | 30.82 s |
| Critic | Astra | 7.171750 | 23.31 s |
| Exporter worker | Luna | 0.140460 | 21.67 s |
| CLI worker | Luna | 0.277170 | 120.27 s |

These are token-based estimates, not verified account deductions. Existing
ChatGPT routing was enforced. Backend model identity is not reported; attribution
uses the requested model and retained argv. Startup diagnostics and context
overhead remain in the raw receipts.

The useful plan returns in 30.82 seconds. The first upheld critique finding
returns 23.31 seconds after its own dispatch, approximately 157.06 seconds after
the first planner request. The durable planning grade is saved at 179.78 seconds.
Native validated-response durations total 196.07 seconds; fixture creation to
stopped result is approximately 429.49 seconds. Wall intervals use retained file
timestamps and include assistant inspection and orchestration gaps. Semantic
grades are labeled and not independent or blind. Desktop presentation and actual
human-response time remain unknown; this incomplete journey supports no model
speed or cost ranking.

## Bounded follow-up

The first handoff correction is useful: this fresh planner obeys the explicit
empty-list contract, and the exporter succeeds without adding a stronger router.
The repaired supplemental runner is not reached in this native journey; its
local and installed replay evidence remains separate.

The completed local correction binds default-output expectations to bytes captured
before any worker runs, inside the already protected oracle. It tests both normal
and empty input, the known bad Luna artifact and unchanged passing Astra artifacts.
No original trial is regraded. See the
[correction design](specs/plan-build/default-compatibility-correction.md) and
[38-check source/installed results](plan-build-default-compatibility-results.md).
Planning-critique transfer is a separate logged opportunity: this worker saw the
protected specification but not the specific critique finding. No automatic
plan rewrite or new approval system is introduced.

Evidence: [summary](receipts/plan-build-handoff-confirmation-native-2026-09-18/summary.json),
[allocation and calls](receipts/plan-build-handoff-confirmation-native-2026-09-18/ledger.json),
[before/after diagnostic](receipts/plan-build-handoff-confirmation-native-2026-09-18/default-compatibility-diagnostic.json),
[protected probe outputs](receipts/plan-build-handoff-confirmation-native-2026-09-18/protected-probes.json),
[bound-input verification at close](receipts/plan-build-handoff-confirmation-native-2026-09-18/final-binding-verification.json).
