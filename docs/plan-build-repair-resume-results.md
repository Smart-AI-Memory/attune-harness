# Bounded repair and resume qualification — September 18, 2026

**Local repair/retest/resume is qualified for the retained failed-test case.**
The original native journey remains failed. Task 8 is still open; native repair
and native final review have not run.

The previous steering boundary required the last operation to be a passed task
probe. It rejected the retained failure after the successful exporter and CLI.
The existing preserve-completed operation now also accepts an ordinary settled
test failure in the next task. Task identities, outputs and dependencies must
remain fixed. The old failed journal stays in history, completed files become
protected inputs, and the new revision requires current Spec acceptance before
repair. Uncertain execution and changed source still block.

## Actual evidence

| Check | Result |
|---|---|
| Source regression checks | 351 passed, no skips |
| Installed wheel regression checks | 351 passed, no skips |
| Final experiment-driver checks | 11 passed after adding preparation-only mode |
| Guard removal | 3/3 detected: settled failure, unchanged task identity/dependencies, source freshness |
| Execution lifecycle checks | Symbol reality and falsifiability PASS |
| Earlier receipts | All 12,378 files captured at this correction's start remain unchanged |
| Additional native calls | 0 |

The installed replay loads the newly built wheel outside the repository. It
reconstructs the failure with the five unchanged original replies, preserving
their references and original source inspection. The original test again reports
three passes and one KeyError. A scripted repair changes only the nested status
access in that test. Four supplemental tests and four protected tests then pass.
A labeled scripted reviewer closes the local build.

The actual Spec collector rejects old and replayed decisions. Execution before
fresh acceptance fails. The completed exporter and CLI retain their bytes and
inodes, and the repaired build writes only the generated test. Unrelated dirty
work stays unchanged. The final handoff includes all three feature files,
completed resume makes no participant call, and a later source edit invalidates
completion. The original failed journal remains unchanged in the new history.

The installed reconstruction reproduces the failure in 2.83 seconds and completes
the local repair experiment in 4.25 seconds. These are local experiment timings,
not native response, desktop display or human response times.

## Limits and reflection

This qualifies a settled ordinary task-test failure after a verified prefix.
It does not implement first-task recovery, uncertain-effect recovery, arbitrary
graph edits or final-review repair. Existing required controls rerun; if they
cannot pass against the retained intermediate state, the repair stays blocked.
The local negative test verifies that behavior instead of weakening the control.

Initial negative checks reproduced the unsupported boundary. Two subsequent
test-harness assertions used the wrong existing error status/shape; inspection
corrected those assertions, with no product relaxation. Their failed XML reports
are retained alongside the final passes. The initial source replay predates the
final boundary-length check; the installed replay and final suites qualify the
final product bytes. No independent model review or provider-backed quality
pipeline was run for this local correction.

The useful lesson is to exercise recovery at real failures rather than repeatedly
restart a full journey. The retained failure exposed a missing host transition;
the correction reuses one owner, its history and its acceptance grammar.

## Next concrete trial

One disposable continuation is prepared at current repair acceptance. Proposed
allocation: **two calls maximum, Luna repair and Astra final review; 5–15 estimated
Codex credits, a 20-credit planning ceiling, existing ChatGPT and $0 new API
dollars**. The repair is supplied the diagnosed nested-field correction, so this
tests guided repair and the connected handoff, not independent fault diagnosis.
Keep all four generated assertions and protected checks. Stop on a substantive
failure; no retry, replacement, fallback or Task 8 acceptance is included.

The previous native allocation is closed. The prepared continuation needs its
own explicit allocation before dispatch. One guided recovery cannot establish
general reliability or qualify a fresh, wholly native first-pass journey.

Evidence: [summary](receipts/plan-build-repair-resume-2026-09-18/summary.json),
[installed replay](receipts/plan-build-repair-resume-2026-09-18/installed-replay/qualification.json),
[guard trials](receipts/plan-build-repair-resume-2026-09-18/guard-removal.json),
[prepared continuation](receipts/plan-build-repair-native-preparation-2026-09-18/prepared-repair.json).
