# Task 1 implementation note

Task: freeze current contracts and behavioral baselines. No production runtime change is part of this increment. The approved scope creates a baseline runner, compatibility tests and a dated result document; original campaigns and concurrent Voyage changes remain intact.

Cases: clean, refuted, unknown and missing-evidence reviews; declined/stale/missing intake; paused and completed replay; stale checkpoints; cancellation and lead transfer; a real local command that writes once, loses acknowledgement and is reconciled without a second effect. Preserve CLI arguments, stdout/stderr, exit codes, original records and hashes. Discover all 18 commands through actual help, with representative valid and invalid routes. Existing native-evidence and Voyage suites cover real-library behavior with injected provider peers; they do not qualify live providers.

Scratch probes before code: local source HEAD is `ce61c18ac396c91e5441ac34f218e41c942df593`; `.venv-voyage312/bin/python` is Python 3.12.13 with forms 0.17.0, verify 0.6.0, RAG 1.2.0, MCP 2.2.0 and the pinned Voyage extras. A disposable deterministic review produced 13 completed operations, verified document links and a separate no-results retrieval outcome. Thirty warm form calls had a 0.905 ms median in that scratch run; this is not the final baseline or a cache-savings claim. Raw scratch evidence is retained at `/var/folders/5t/bzsz5qd17pd7h_wwwd5qjbnw0000gn/T/harness-task1-scratch-uqt8pxuq/scratch.json`.

The runner will use a caller-selected, newly created output directory, pinned local libraries and poison native/Voyage constructors. It will record source and fixture hashes plus actual call/effect counts, measure form schema construction, rendering, accepted-answer validation and fresh-process CLI latency separately, and retain raw samples. User-visible host transport and human think time remain unmeasured. No environment upgrades or paid calls.

Rejected alternative: snapshot only mocked return values or current help text. That would miss changes to real forms, exit semantics and recovery records. Another rejected alternative is to copy old runs into new directories and call them resumable: current records bind absolute paths/ownership, so immutable snapshots remain evidence while the original retained fixture locations remain the replay authority.

Verification: the new compatibility suite, the current review/recovery/native-evidence/Voyage families, actual baseline runner output, and a focused guard-removal probe if needed to demonstrate that new compatibility assertions detect semantic changes. The generic paid quality workflow cannot run under Task 1's zero-provider contract; report concrete offline checks and any omitted review honestly.
