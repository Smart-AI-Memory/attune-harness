# Repair software qualification — Task 7

The fix policy uses the same task intake, writer lease, authoritative checkpoint, participant dispatch, operation journal, subprocess supervision and error boundary as assessment. The legacy adapter still reconstructs its original packets and operation keys. No automatic synthesis, provider fallback, paid review or broad native write grant was added.

## Observed behavior

Twenty-three repair policy tests pass: none/requested/required review; failed-before/passed-after actual code; every operation boundary replay; wrong/no-op fixes; initially passing probes; rejecting/uncertain/missing/foreign review; final byte and oracle changes; lost write/probe acknowledgement; and native read-only transport using a fixture. A reviewer receives final before/after code and immutable probe results, with no worker self-verdict. Requested review cannot silently disappear. Native fixture results are not model qualification.

The applicable software suite passed **1,126 tests, with 25 skips**, before two additional repair negatives and the final intake-display change. The final focused intake/repair/compatibility suite passed **117 tests** (including all 23 repair tests). Earlier failing runs are retained: the initial policy mishandled the process adapter's normal `nonzero_exit`; the next exposed tuple/list differences in persisted probe output. Both are fixed. Task 6's effect receipt count is corrected to 29 effect cases; its total 136 passing checks was accurate.

Thirteen targeted mutations were detected: five assessment/replay/isolation guards, four scope/effect guards, and four repair acceptance/review guards. Distinct failing selected cases per family: 13, 4 and 8 respectively. These are targeted guards, not an exhaustive mutation score.

## Installed artifact

Fresh `.venv-task-repair-final310`, local pinned wheels only. Outside-checkout isolated consumer ran 22 processes; all **41 module hashes** matched installed wheel and source. Assessment still completed solo/team pause/resume. Repair's intended patch completed, a wrong patch failed and a rejecting reviewer blocked acceptance. Five separate repair command-peer calls occurred, with no duplicate writes during continuation. Zero native/provider calls.

Final wheel: `dist/task-execution-repair-final/attune_harness-0.1.0.dev13-py3-none-any.whl`; SHA-256 `48e9af5c9877f2f1e8f1ebb39724203787a8fe9b740dfd0fcd5fd4e6ce5f6a15`. Previous candidate artifacts/environments are preserved. The development version was not published or promoted. Earlier repair candidate also passed 12 legacy installed review and 23 installed recovery checks; the final candidate only changes visible intake display and passed all new installed consumers.

Raw receipts under `docs/receipts/unified-task-execution/`: `task7-installed-final/summary.json`, `task7-applicable-suite.xml`, `task7-final-intake.xml`, `task7-repair-final-v2.xml`, `task7-{assessment,effect,policy}-mutations/summary.json`, and `task7-legacy-{review,recovery}.json`.

## Limits

Qualified here: macOS arm64, Python 3.10 installed software and Python 3.12 source tests. Repair requires an exclusively owned bounded checkout with a local .git directory, existing regular UTF-8 files, an explicit immutable probe and protected oracles. No create/delete/rename or Windows effect qualification. It is not a security sandbox for hostile same-user processes or untrusted command peers/probes. A repair may leave applied files when its later probe or review fails; no rollback is claimed.

Full native provider/semantic/economic qualification remains Task 8. The host A2A startup failures and historical campaign artifact mismatch from Task 3 remain documented exclusions. No full-spec or production-provider promotion follows from these offline receipts.
