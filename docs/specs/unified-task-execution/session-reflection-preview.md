# Session reflection form preview

Synthetic example only. No user answer, approval, or memory write occurred. The host composes the same candidate template into one triage list; categories and candidate count are dynamic.

## Reflect — Example project

*Coverage: Synthetic transcript, messages 1–4; complete fixture. Review candidate insights before saving them. Unanswered items remain unreviewed.*

1. **Which insights should we keep, edit, revisit or discard?**
*Keep saves the exact item to its displayed destination when supported and authorized. It does not start work or approve its suggestions.*
Rule each item as one of: `Keep` / `Edit` / `Revisit` / `Discard`
- **Opportunities: synthetic candidate 1** `Opportunities`
      Source: Synthetic message 3: We keep answering the same project questions.
      Basis: Inference from the reported repeated questions; no latency measurement yet.
      Why it matters: Could reduce repeated input; the latency benefit is unmeasured.
      Keep destination: Project notes: session-insights.md (illustrative destination only)
- **Key facts: synthetic candidate 2** `Key facts`
      Source: Synthetic message 3: We keep answering the same project questions.
      Basis: Inference from the reported repeated questions; no latency measurement yet.
      Why it matters: Could reduce repeated input; the latency benefit is unmeasured.
      Keep destination: Project notes: session-insights.md (illustrative destination only)
- **Pushback: synthetic candidate 3** `Pushback`
      Source: Synthetic message 3: We keep answering the same project questions.
      Basis: Inference from the reported repeated questions; no latency measurement yet.
      Why it matters: Could reduce repeated input; the latency benefit is unmeasured.
      Keep destination: Project notes: session-insights.md (illustrative destination only)
- **Decisions: synthetic candidate 4** `Decisions`
      Source: Synthetic message 3: We keep answering the same project questions.
      Basis: Inference from the reported repeated questions; no latency measurement yet.
      Why it matters: Could reduce repeated input; the latency benefit is unmeasured.
      Keep destination: Project notes: session-insights.md (illustrative destination only)
- **Open questions: synthetic candidate 5** `Open questions`
      Source: Synthetic message 3: We keep answering the same project questions.
      Basis: Inference from the reported repeated questions; no latency measurement yet.
      Why it matters: Could reduce repeated input; the latency benefit is unmeasured.
      Keep destination: Project notes: session-insights.md (illustrative destination only)
- **Lessons and patterns: synthetic candidate 6** `Lessons and patterns`
      Source: Synthetic message 3: We keep answering the same project questions.
      Basis: Inference from the reported repeated questions; no latency measurement yet.
      Why it matters: Could reduce repeated input; the latency benefit is unmeasured.
      Keep destination: Project notes: session-insights.md (illustrative destination only)

---
Reply by filling the `answers` values below, or with shorthand lines — `field_id: value` or `N: value` (field number); a triage row is `field_id.item_id: disposition`; a ranking is a comma list in order (`field_id: b, a, c`) or one slot per line (`field_id.1: b`); an assumption row is `field_id.item_id: accept`, `field_id.item_id: reject`, or `field_id.item_id: edit: <text>`:

```json
{
  "__elicitation_response__": true,
  "title": "Reflect — Example project",
  "answers": {
    "insights": {
      "example-001": null,
      "example-002": null,
      "example-003": null,
      "example-004": null,
      "example-005": null,
      "example-006": null
    }
  }
}
```
