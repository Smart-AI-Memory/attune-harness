# Task 8 offline preparation note

Freeze the candidate wheel/source identity, configured model/settings, held-out fixture bytes, comparison arms, call ceilings, order, cache conditions, grading rubric and exclusions before live execution. Preparation, verification and grading-integrity checks must never construct a provider. A separate explicit first-spend decision and current account/profile checks are still required; an auto-run software task result is not permission for native trials.

Actual scratch: changing a frozen case's expected unknown outcome to clean changed its SHA-256 and was detected. Earlier installed consumers verified all 41 candidate modules; their source hashes bind this protocol. Cases below are a new synthetic screening set rather than the Quartz implementation fixture. They are held out from implementation tests, not a blinded production distribution. Native command identity/access and billing remain unqualified.

Prepare two configured lead families, Claude Fable/Astra reviewer and Codex Astra/Sol reviewer, using models already named in repository profiles. Compare retained legacy review, unified solo and unified independent review on six matched assessment cases. Prepare direct single-worker and unified solo/required-review repair on four matched cases using identical host replacement/probe boundaries. Two repetitions would cost more; freeze one screening repetition and retain that statistical limitation. The direct-repair baseline omits task intake/policy composition while reusing the same host dispatch/effect/probe boundaries; its offline rehearsal is separate from native qualification.

Record 92 maximum participant calls (30 configured Claude, 62 Codex) before early failures; no automatic retries or paid grader. Human blinded grading and correction-time receipts are required. Token/billed cost remains unknown before exact native admission/billing checks; a numerical dollar estimate would be invented. The proposed protocol is therefore held, not executable authorization. No allocation from the shared Claude budget is made here.

Offline tooling validates immutable hashes, exact trial/grade sets, participant counts, complete failure retention and unknown-cost behavior. The runner prepares/verifies, explicitly admits execution, exports anonymous grading packets and audits retained results. Execute requires protocol-bound call/spend/profile approval and cannot reopen an existing run directory. Preparation never constructs a provider. Native dispatch remains unrun until admission. This does not complete Task 8. Rejected: silently launching available credentials, selecting another model after failure, or scoring a smaller successful subset as the frozen campaign.

Final offline probe: 49 cases pass. A simulated native process exposed that the original reference filename failed the keyword retriever’s minimum score. An isolated real-library probe established a `maple-reference.md` path match, then all six assessment cases across legacy/solo/independent arms passed through the real native adapter with injected process responses. Earlier failures remain retained. This tests transport and evidence availability, not generated review quality.

The runner journals each native dispatch before constructing its transport, retains raw stdout/stderr and observed usage metadata, and preserves missing tokens/billing as unknown. Reasoning output is recorded separately and never added twice. Anonymous grading exports omit explicit model/role identities; prose and arm shape can still reveal them. Withhold the separate mapping file from graders. The final packet and exact reproduction commands are in [verification](verification.md).
