# Task 6 repair effect receipt

Implemented the POSIX existing UTF-8 replacement profile, immutable probe configuration and explicit subprocess environment. Shared RecoveryCursor journals each file; task_runtime exposes the same journal for acceptance probes. Windows repair effects are unavailable pending native handle qualification. This is an exclusively owned dedicated-checkout workflow, not containment against hostile same-user processes or untrusted commands.

136 focused tests passed, including 30 real repair effect cases and existing process/task/legacy recovery checks (`task6-final-v2.xml`). A real child process exits after replacement but before acknowledgement: the parent sees a dispatching journal, reconciles actual after-bytes and resumes without changing that file's inode again. Multi-file partial patches remain truthful. Wrong/no-op repairs, unsafe paths, symlink/hardlink aliases, stale preimages, protected oracle/state, unrelated modifications, output/encoding limits, post-write corruption and inherited environment leakage are covered.

Four disposable guard mutations (scope, preimage, oracle, after-image) were detected by four distinct cases; original control passed (`task6-mutations/summary.json`). No provider/model calls. Full integration, review obligations and installed repair are Task 7; this receipt does not claim those are complete.
