# Task 8 correction note — review findings semantics

The live frozen candidate has a reviewer prompt/validator mismatch. The arithmetic Codex required-review trial and lower-bound Claude required-review trial each applied a probe-passing repair. The independent reviewer returned approve plus positive explanatory findings; `execute_repair` then rejected any nonempty findings list. This was not covered by deterministic peers, which always emitted empty findings on approval. The recorded native outputs are the actual probe; no extra diagnostic model calls are needed to establish the mismatch.

Cases: approve with no blocking issue must use an empty findings list; reject/uncertain must retain defects or material uncertainty; approval with nonempty findings must still fail closed; artifact/probe mismatch, missing review and changed final bytes must still fail. Passing the probe is bounded evidence and must not invite a general correctness claim. Ordinary disclosure that the reviewer did not rerun the supplied host probe is not itself a newly discovered defect.

Correction after the immutable live candidate finishes: explicitly define findings as blocking defects or material uncertainty, prohibit positive summaries/nonblocking caveats in that field, and show the approval shape `verdict: approve, findings: []`. Keep the schema, validator, journaling and evidence isolation unchanged. Run the existing repair/effect/assessment/continuation tests and independently install the rebuilt wheel. A prompt-only correction passing software tests does not establish changed live-model behavior; retain native requalification as pending.

Rejected: accepting any nonempty findings whenever verdict says approve, which would discard contradictory or unresolved objections; editing the original trial/grade or replacing failed results; silently updating the installed candidate during its frozen campaign; spending beyond the declared 92-call comparison for an unannounced rerun. A later native rerun must identify the new artifact and preserve the old outcomes.
