# Unified task runtime — verification receipt

**2026-09-16: Tasks 1–7 accepted on offline evidence; Task 8 prepared, not accepted.** Both slices have bounded software qualification. Native quality, human correction effort and economic comparison remain unqualified. Full-spec completion and production-provider promotion are not claimed.

## Delivered behavior and artifact

`review`, `fix`, `status` and `resume` use one task identity and authoritative checkpoint. Assessment has solo and isolated independent-review policies. Repair journals scoped existing-file replacements, requires a failed-before/passed-after immutable probe, and enforces requested/required review against final artifact and probe digests. Unknown effects remain unresolved until explicit reconciliation. No rollback is claimed.

The legacy review loop moved into the shared runtime; legacy packet construction preserves accepted operation identities. Assessment and repair share participant dispatch, recovery cursor, persistence and failure handling. There was one legacy orchestration loop, not 18 engines. Existing transports and specialized paid retrieval journals remain. Zero legacy commands were retired; no maintenance or model-cost savings are inferred from renaming.

Runtime commit: `0555ed7`, following baseline `e546cc6` and separate implementation commits. Final development wheel: `dist/task-execution-repair-final/attune_harness-0.1.0.dev13-py3-none-any.whl`, SHA-256 `48e9af5c9877f2f1e8f1ebb39724203787a8fe9b740dfd0fcd5fd4e6ce5f6a15`. All 41 installed modules match wheel and source. Fresh `.venv-task-repair-final310` consumes local pinned wheels outside the source tree. No package release, push or PR occurred.

## Actual checks

| Evidence | Observed result |
|---|---|
| Applicable software suite | 1,126 passed, 25 skipped before final two repair negatives/intake display change |
| Final intake/repair/compatibility checks | 117 passed, including 23 repair policy cases |
| Installed consumer | 22 outside-tree processes; assessment and repair outcomes, wrong-patch and rejected-review negatives; no duplicate writes |
| Targeted guard mutations | 13/13 detected: five assessment, four effect, four repair-policy guards |
| Task 8 tooling | 49 passed; real local effects, all assessment arms through simulated native process responses, admission/integrity and grading failures |
| Refreshed command baseline | 54 captures, all 18 legacy commands, zero provider attempts |
| Preserved historical evidence | All 154 original baseline artifacts unchanged |

Receipts: [assessment](assessment-receipt.md), [repair](repair-receipt.md), [original baseline](baseline.md). Raw local evidence is under `docs/receipts/unified-task-execution/`: `task7-installed-final/summary.json`, `task7-applicable-suite.xml`, `task7-final-intake.xml`, `task7-{assessment,effect,policy}-mutations/summary.json`, `task8-offline-final-v8.xml`, `task8-command-compatibility/summary.json` and `task8-final-integrity.json`. Earlier failed checks remain retained.

The broad run excludes `tests/test_a2a.py` after this host’s local server startup failures, and `tests/test_review_quality.py`, whose seven fixtures require their preserved historical installed artifact. Neither exclusion is represented as passing. Platform skips remain visible. The scripted review/fix/status/resume journeys are software checks, not a human usability study.

Final installed intake medians: cold 2.266 ms, warm 2.021 ms, bypass 2.234 ms; fresh CLI process 129.881 ms. These local samples justify neither persistent caching nor a product speed claim. No model calls were avoided by template caching.

## Complete legacy command disposition

| Commands | Disposition and evidence |
|---|---|
| `review-form`, `review` | Retained accepted-form and positional legacy mode; review also supports task intake |
| `inspect-review`, `resume-review` | Retained legacy inspection/checkpoint continuation |
| `reconcile-review`, `transfer-review`, `cancel-review` | Retained reconciliation, transfer and cancellation; task controls added separately |
| `retrieve`, `verify` | Retained; actual local-library captures |
| `extension`, `code-config` | Retained discovery/configuration captures and existing behavioral suites |
| `index`, `retrieval-task` | Retained; invalid-input captures and existing Voyage behavioral owners |
| `triage-check`, `repair-economics`, `github-checks` | Retained bounded suggestion, ledger validation and complete-export behavior |
| `mcp-inspect`, `mcp-serve` | Retained local inspection and invalid-intake behavior; existing MCP service tests |

New primary help emphasizes review/fix/status/resume. Advanced `reconcile-task`, `transfer-task` and `cancel-task` use the same recovery machinery. Plan/build/ship and the approved reflect follow-on are not implemented by these eight tasks.

## Qualification limits by profile

| Profile | Decision |
|---|---|
| macOS arm64 Python 3.12 source; Python 3.10 installed command peers | Software qualified within recorded checks |
| POSIX repair profile | Existing regular UTF-8 replacements in an exclusively owned bounded checkout with local `.git` directory; no linked worktree/create/delete/rename qualification |
| Windows/Linux repair | Not qualified by this host’s receipts |
| Configured Claude Fable worker/lead + Astra reviewer | Native availability, behavior and economics pending |
| Configured Astra worker/lead + Sol reviewer | Native availability, behavior and economics pending |

The repair profile is not a security sandbox for hostile same-user processes or untrusted probes. Applied files may remain if a later probe or reviewer rejects the result. Passing a probe means verified within that probe’s scope, not general semantic correctness.

## Task 8 frozen comparison, held offline

The current packet is `docs/receipts/unified-task-execution/task8-frozen-protocol-qualified/`, protocol SHA-256 `536505510f36feeaae4cc5c36a642e3e754b4f87cef34fc67d69803a1cac6057`. It freezes runner/artifact/module identities, new synthetic cases, configured settings, trial order, grading keys, cache conditions and call limits. Earlier packets are retained but have stale runner hashes.

Six assessment cases compare legacy/solo/independent-review; four repair cases compare direct/solo/required-review. Two configured lead families give **60 trials, at most 92 participant calls: 30 Claude and 62 Codex**. One screening repetition, no automatic retries, no paid grader. This is not a population accuracy study.

```sh
.venv-task-repair-final310/bin/python -I experiments/task_execution/campaign.py verify docs/receipts/unified-task-execution/task8-frozen-protocol-qualified
.venv-voyage312/bin/python -B -m pytest tests/test_task_campaign.py -q -p no:cacheprovider
```

Execution requires a separate exact-protocol admission record with explicit approval, a current numerical estimate, budget review and confirmed native profiles. None was created. The shared Claude budget is $40 with an existing calibration reservation; this campaign receives no allocation. Current access/billing and trial token estimates are unknown. Existing unrelated authorizations do not authorize these trials.

The runner persists dispatch intent, raw native outputs, observed token metadata, failures and elapsed time. Missing billed cost stays unknown; reasoning tokens are not counted twice. Human grading packets hide explicit identities, with a separate withheld mapping; prose or arm shape may still reveal the model. Complete retained trial/grade sets and matched quality floors are required. There are no live results or human grades yet. **Offline preparation cannot accept Task 8.**
