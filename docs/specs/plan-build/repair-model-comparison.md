# Compare repair workers under one qualified handoff

Status: local preparation authorized by Patrick's “go” and A response on
2026-09-18. Native allocation pending. This supersedes the pending two-call Luna
confirmation; its frozen packet and all previous failures remain unchanged.

Question: which of Luna, Sol and Astra merits the next connected repair trial?
Compare the current one-response, no-tool worker profile, not unrestricted agent
capability or memory sorting. A model's repair result does not revoke evidence for
its other roles. Sol is a challenger, not a presumed winner; Astra is a reference,
not an infallible grader.

Freeze three cases before any response: the retained nested-status test failure;
a new exporter defect that filters out unknown/refuted findings; and a new CLI
defect that selects JSONL by default, breaking captured default output. The latter
two are newly injected defects in disposable copies of previously checked code,
not newly discovered production bugs. All receive actual failure excerpts,
expected behavior, exact write scope and the same corrected worker contract.
The known case includes its diagnosed access path; the two fresh cases include
symptoms and requirements but no reference patch. Keep their results separate.

Use three models, three cases and two fresh repetitions: 18 native worker calls.
Interleave models within each case/repetition using a frozen randomized order.
Every model receives the identical serialized turn for that case, high reasoning,
Standard service, one response, no tools, no fallback and no response repair.
Record requested versus reported identity and any inherited host-context limits.
This is a shared Harness profile comparison, not a universal model ranking.

Before spend, reproduce each failure and show its locally authored reference
repair passes the four supplemental and four protected tests. Evaluate through
the existing Spec collector, build journal, effect decoder and test probes in
fresh disposable owners. A scripted final reviewer only finishes local replay;
it cannot qualify native review or the complete native journey. Keep reference
repairs and expected grades out of native working directories and turn evidence.

Success requires a valid scoped proposal, passing actual tests, preservation of
expected behavior/assertions and unrelated work, and no unsupported guarantee.
Session-assistant semantic inspection is required before executing any native
source and is disclosed as non-independent. Score every original failure; never
patch a response to convert its grade. Preserve the existing stale-source and
no-repeat checks. Test rejection with unchanged replies, weakened tests and a
wrong-scope proposal, and check a correct alternative repair is accepted.

Measure contract validity, first-attempt correct repairs by case, time until
test-backed disposition, native latency, local evaluation time, input/cache/output
tokens and estimated credits including failed attempts. Divide total cost by
successful repairs only when successes exist; zero successes means undefined.
Desktop presentation delay and human attention time remain unknown. Report all
six outcomes per model rather than averaging away a missed behavior.

Continue independent screen cases after a resolved ordinary model/test failure;
that is an observation, not permission to retry it. Stop the allocation for
unknown dispatch/usage, changed inputs, authentication ambiguity, host enforcement
failure or exhausted call/credit allowance. Count transport failures and unused
slots. The previous journey's stop-on-first-task-failure rule still applies to
that closed allocation; this new screen's policy must be explicitly approved.

Freeze the call/cost estimate and current published credit rates in the packet.
The new screen needs its own allocation; unused prior credit ceilings are not
silently transferred. No native final reviewer, Task 8 acceptance, release,
production route change, live memory access or installation change is included.

Choose a next-trial candidate only if all six scoped repairs pass without a
serious preservation/enforcement miss. With a tie, compare observed time and cost
and retain the cheaper eligible candidate provisionally. Two repetitions cannot
establish reliability or statistical superiority. If no model meets the floor,
review the shared handoff/setup and failure classes before more prompt tuning.
Confirm any provisional choice on the connected journey afterward.

Rationale: the previous failure mixes a real bad reply with a handoff clarity gap.
Another Luna-only success would not answer Patrick's model-selection question.
A larger benchmark or a new router would widen scope prematurely. Equivalent
tasks, scoring and harness conditions follow OpenAI's
[evaluation guidance](https://openai.com/index/trustworthy-third-party-evaluations-foundations/).
Its warning about broken environments applies directly to the earlier startup
and import failures; those cannot establish that a model lacks repair capability.
