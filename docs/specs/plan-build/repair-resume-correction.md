# Qualify repair after a settled task-test failure

Status: locally qualified: 351 source and 351 installed checks, 3/3 guard
removals detected, and an installed retained-failure repair replay completed.
See [results](../../plan-build-repair-resume-results.md). Continues Task 8
under the existing non-high local authorization, reaffirmed by “approve and
continue.” No native calls are part of this correction.

The retained fresh journey has a verified exporter and CLI, followed by a
generated test whose assertion uses the wrong envelope field. The existing
preserve-completed revision operation permits correction only immediately after
a passed task probe; it rejects this settled failed probe. Repeating the entire
journey would avoid qualifying the recovery boundary.

Extend that existing operation only for a stopped `needs_revision` build whose
last journaled operation is the next task's completed, ordinary nonzero-exit
verification failure. Require a verified unchanged prefix, fresh source/config/
interpreter evidence and no unresolved effects. Keep the pending task identities,
dependencies and outputs fixed; permit objective/check clarification. Capture
the failed task's existing output as the repair preimage, preserve the full
failed journal in history, and clear the old acceptance. Reaccept through the
actual Spec collector before any new worker proposal. Protect completed outputs
and use the existing probes and final review. No new command or gate is needed.

Qualify using the original five retained replies in a disposable reconstruction,
then one explicitly scripted assertion repair and a scripted final reviewer.
Rerun supplemental and protected tests. Verify stale/replayed decision rejection,
no repeated completed worker/effect, preserved dirty work, current completed
handoff, and stale-source rejection. Retain the original campaign unchanged and
failed. Local replay does not qualify native repair or native final review.

Negative cases cover changing the failed task's identity/scope/dependencies,
repair before a verified prefix, source drift, uncertain execution, and a final
review failure. Target guard removals at the new boundary and retained freshness/
authority protections. Run source and installed checks and the existing lifecycle
gates before reporting this correction qualified.

Reject clearing the failed probe, editing an accepted journal in place, applying
the test fix outside the effect owner, or reusing the old passing checks as new
completion. Broader rework, first-task failure and final-review repair remain
outside this bounded profile.
