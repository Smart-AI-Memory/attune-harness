# Repair contenders — expanded local preparation

Status: local preparation authorized; paid allocation pending. Patrick selected
another trial, questioned the small sample and directed that only Luna and Sol
compete. This replaces the proposed three-model repeat. Preserve the completed
18-call screen and all earlier failures.

Question: does Sol deliver a repair-quality advantage worth its cost in the
current bounded worker role? Prepare 54 calls: nine cases, three attempts per case,
two candidates. Retain three anchors; add empty-output, status precedence and
record-envelope defects, plus paired header-loss, CLI-compatibility and generated
assertion defects. Repetitions measure variation on a case; they do not create
54 distinct or independent problems. All cases remain in one documentation
feature and cannot represent the whole Harness workload.

Reuse repair_models.py for Spec ownership, scoped effects, actual supplemental
and protected execution, inspection binding, no-repeat and stale-source checks.
An offline preparation script makes frozen fixtures and reference repairs; it
must forbid native dispatch. Reproduce every defect; run all eight checks after
each full reference repair; require partial repairs of the paired defects to fail.
Reference patches stay outside native working directories and supplied turns.
Existing source, installed profile, original receipts and live stores stay intact.

Freeze inputs, balanced model order and all 54 slots before spend. Use high
reasoning, Standard service, one response, no tools and no retries. Keep anchors
separate from the six new cases in analysis. Grade originals using the existing
host contract, actual tests and Astra's semantic inspection in this session, as
Patrick requested; neither candidate grades itself. That inspection is not
independent or strictly blinded. No separate Astra native call is proposed; the
worker-call estimate excludes ordinary session review usage, which is not metered
by the experimental ledger.

Preserve the prior screen's conservative eligibility principle: every scoped
repair must pass without a serious preservation miss. A candidate needs 27/27
in this screen to qualify for the next connected trial. Report all outcomes by
case and attempt, including failures and costs. Ties favor the cheaper eligible
candidate; no eligible candidate means analyze failure classes before another
experiment. No adaptive extra calls, early declaration of success, or automatic
production routing. Ordinary model/test failures are scored; unsafe proposals
are rejected without execution. Uncertain usage, source drift, authentication
ambiguity, host enforcement failure or exhausted allocation stops dispatch.

The earlier 6/6 result supports feasibility, not high reliability. Even an ideal
fixed independent binomial experiment with six successes permits an approximately
39% failure-rate upper bound at one-sided 95% confidence. Our clustered, selected
cases do not satisfy the conditions for transferring that calculation to real
work. More calls and broader cases help different questions; neither removes the
need for a connected native journey. See [NIST exact intervals](https://itl.nist.gov/div898/handbook/prc/section2/prc241.htm)
and [OpenAI evaluation guidance](https://openai.com/index/trustworthy-third-party-evaluations-foundations/).

Rejected alternatives: another identical three-case screen adds limited coverage;
keeping Astra spends on a model Patrick is not considering for this slot; adding
new repair protocols or escalation output types confounds the comparison with an
interface change. Broader cross-feature and multi-file qualification remains
valuable after this bounded role comparison. Prepare the concrete cost estimate
and durable decision before any paid execution.
