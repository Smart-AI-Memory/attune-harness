# Task 8 — bounded outcome comparison

Status: offline preparation complete; native allocation unapproved. Tasks 1–7 remain
accepted. Task 8 cannot be accepted until its separately authorized outcome
evidence is evaluated.

The first comparison screens two roles on the selected JSON Lines feature:
planning after a human corrects an earlier verified-only idea, and implementing
the first export step without dropping unknown/refuted findings or advisory
notes. Planning must retain the corrected goal, exact scope and acceptance
criteria. Building must satisfy the protected behavioral oracle. Neither valid
JSON nor a passing generated test establishes those outcomes.

Freeze two matched prompt arms: the current full planning/build protocol and a
concise contract baseline. Inputs, response schema, model settings and host
controls remain identical. The baseline is an experimental comparator, not a
claim about an earlier released implementation. No safety guard is removed.
Use Luna, Astra and Fable, two repetitions per role and arm: 24 calls total.
The exact profiles, prompts, order, source hashes and acceptance floors will be
retained in the preparation receipt before dispatch is requested.

Offline cases must establish that correct plans survive validation; wrong goals,
scope and authority fail; a well-shaped but semantically wrong plan remains a
semantic review obligation; and a wrong serializer fails the fixed oracle even
when its trivial generated test passes. Existing Tasks 5–7 already exercised
actual dependent effects, mid-plan steering, stale decisions and installed verbs.
These preparation probes must be run and their actual results recorded before
requesting the allocation. They are disposable local experiments.

Reject expanding this first trial into an autonomous full journey or a model
grader committee: the bounded role screen should reveal protocol defects and
promising roles first. It cannot qualify native critics, reviewers, arbitrary
projects, production reliability or a live installation. A successful role screen
would justify proposing that next connected experiment, not automatic adoption.

Score schema validity, goal fidelity, constraints, artifact behavior, unsupported
claims, unnecessary questions/blocks, useful alternatives and optional advice
separately. Blind model/arm labels before semantic scoring; retain the rubric and
rationales. The session assistant's semantic grading is disclosed, not described
as independent model review. No critical miss or enforcement miss is acceptable;
candidate repetitions must both pass and must not regress against a correct
matched baseline. With only two repetitions, report counts, not reliability rates.

Measure dispatch-to-validated-output and complete evaluation time, calls and
reported tokens. A schema-valid but wrong output is not a useful result. Record
presentation separately; unavailable desktop timing stays unknown. Human response
time is excluded. Stop on unresolved calls or usage, auth/routing ambiguity,
critical enforcement failures, or allocation exhaustion. No automatic retry,
model fallback, new API dollars or release authority follows from preparation.

## Completed offline evidence

The actual preparation suite passes 18 checks. Positive and deliberately wrong
serializer fixtures ran the complete protected oracle with the fixed CLI driver;
both trivial generated tests pass, while the wrong serializer is rejected. Host
source and captured-input changes invalidate the packet. Isolated removal of each
of those two freshness comparisons is detected (2/2). The structurally valid wrong
plan is retained as an explicit semantic-review control, not a host enforcement
defect. No native dispatch occurred.
