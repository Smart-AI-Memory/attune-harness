# Local correction of the connected experiment handoffs

Status: local Task 8 correction qualified; separate six-call confirmation approved
with a 40-credit planning limit. Task 8 is not accepted.

The closed connected comparison retained two distinct failures: Luna omitted
required choices, and Astra's supplemental discovery loaded installed Harness
instead of the disposable source tree. The unchanged Astra sources pass when
the import context is correct. Preserve the original campaign and its grades.

## Bounded changes

- Clarify the v2 planner protocol: retain every required field, including
  `choices: []` when there are no unresolved material choices. Keep the current
  strict decoder and all human-authority checks. Legacy protocol behavior stays
  unchanged. This is guidance, not evidence of improved native compliance.
- Add a protected runner to the connected fixture. It selects checkout/src,
  verifies the actual package/documentation/exporter origins, discovers ordinary
  supplemental unittest tests, and fails on empty discovery. Freeze its bytes
  with the existing oracle inputs. Use the same clean subprocess environment;
  do not allow arbitrary PYTHONPATH in the product runner.
- Use the existing stepped journey driver and installed Spec collector for an
  offline replay of retained original Astra plan/critique/worker replies. Use a
  clearly labeled scripted final reviewer, forbid native dispatch and preserve
  original replies. Exercise correction/reacceptance, original generated tests,
  protected checks, completed resume and stale-source rejection.

## Acceptance evidence

Ordinary tests must run against the produced files even when an installed/shadow
package is available. Wrong import origins, missing exports, failing tests and
zero collected tests must not produce success. A changed protected runner must
invalidate accepted execution. Remove each new runner protection independently
in disposable copies and demonstrate that its corresponding check detects it.
Missing, null, malformed and participant-selected choices still fail; an explicit
empty list can stage only as an unaccepted draft. Retain actual source and
isolated-wheel results and verify earlier receipt manifests remain unchanged.

The existing native response transport constrains an outer text envelope; the
role payload is decoded and validated by the host. Changing provider-specific
structured-output transport is outside this correction. Silent defaults would
invent the absence of a human decision and are rejected. Making generated tests
repair the runner's environment would perpetuate the hidden dependency and is
also rejected.

Task 8 stays open: local replay is not fresh native response quality or native
final-review evidence. Prepare any next paid allocation separately, after the
local corrections have their own evidence.

## Next confirmation preparation

Prepare one fresh candidate journey, six calls maximum: Luna planner plus three
workers, Astra critic and final reviewer. Retain the earlier four passed review
controls rather than purchasing replacements. Do not claim a fresh matched model
comparison. Expected 15–30 estimated Codex credits, 40-credit planning ceiling,
existing ChatGPT and zero new API dollars. This uses the observed roughly seven
credits for Astra critique and allows a larger final-review context; full worker
outputs can exceed the earlier small Luna examples.

Adapt only the experimental runner's prepared-packet selection and allocation
description so the new immutable packet cannot inherit the closed sixteen-call
allocation. Verify those changes with injected native envelopes, unknown/stale
input negatives and budget checks, without real provider dispatch. The original
campaign receipts remain immutable. The instruction to continue local work did
not itself approve native calls. Patrick subsequently accepted the proposed
40-credit limit in conversation; record that reply with the separate admission.
