# Preserve captured default output in the experimental oracle

Status: locally qualified after the closed four-call confirmation: 38 source
checks, 38 installed checks, 1/1 guard-removal detection and a retained-reply
connected replay. See [results](../../plan-build-default-compatibility-results.md).
No additional native allocation, product feature or Task 8 acceptance is implied.

The original Luna CLI proposal changes an em dash to an en dash in markdown().
That changes the default JSON bundle, contrary to the accepted requirement.
The original protected oracle passes because it compares the new CLI output
with the new implementation's markdown() result. A direct pre/post subprocess
comparison confirms the regression; the original reply and failed journey stay
unchanged.

At disposable fixture creation, run the captured original module on both the
normal and empty sample using the exact interpreter and explicit fixture import
path. Embed the two stdout strings in the already protected acceptance.py before
acceptance and git baseline capture. At verification, compare default stdout
against those captured strings as well as checking internal receipt consistency.
The baseline is captured once before any worker output, never regenerated by the
verification check. Missing capture must fail. No additional state store or gate
is needed; existing frozen oracle hashes protect the embedded baseline.

Qualify with the unchanged original passing Astra artifacts, the unchanged fresh
Luna regression, a default-empty-output regression and a missing baseline. Remove
the external-baseline comparison in a disposable copy and demonstrate that the
known regression escapes again. Run the existing offline connected replay and
relevant experiment suites with native dispatch forbidden. Preserve all earlier
receipts and distinguish this new oracle from the original trial's oracle.

Reject treating punctuation as permission to change the promised output, asking
the worker to redefine expected results, or repairing the original trial to make
it pass. This narrow fixture does not certify arbitrary input compatibility,
representative workload performance or native completion. The critique-transfer
opportunity remains separately logged; it is outside this correction.
