# Fresh complete journey after the compatibility correction

Patrick directed “qualify the fresh complete journey. pushback?” after the
four-call confirmation stopped. This authorizes one fresh run of the same
six-call journey. Patrick then clarified “you're authorized to spend 40 more
credits” and “continue the current task.” The fresh planning allowance is
40 additional credits; the prior 7.744460 credits stay separate. This supersedes
the conservative shared-ceiling interpretation before any dispatch. This is a
new admission; the failed campaign remains closed and unchanged.

Use Luna for the planner and three ordered workers, Astra for critique and final
review. Expected fresh cost remains 15–30 estimated Codex credits, existing
ChatGPT and zero new API dollars. Keep the same pre-call reservations, maximum
six calls, no automatic retries/replacements and no provider fallback. Unknown
usage, changed admitted inputs or a substantive failure stops the run. The
planning meter cannot guarantee a hard per-response invoice cap.

Use the actual installed work/effect owners and Spec collector, the qualified
supplemental import runner, and the strengthened oracle with normal and empty
default output captured before worker effects. Preserve original model replies,
selected file scope, unrelated dirty fixture work and protected inputs. Inspect
each proposed source before executing it. Retain the four existing review
controls as prior evidence; they are not fresh control calls.

Success requires the complete original planning, critique, staged acceptance,
exporter, CLI, supplemental tests, protected final checks and native final review.
At the verified first step, exercise the same synthetic correction/reacceptance
and replay rejection, preserve completed work, then complete without repeated
participant calls. A later source change must invalidate completed evidence.
The original native reply must satisfy the default-output requirement; neither
the expected output nor a failed reply may be repaired within this trial.

Freeze the packet, current source and qualified evidence before dispatch. The
preparation helper binds the previous closed ledger as history and the current
40-credit allocation as a separate ceiling. Verify the prepared budget and
existing admission controls locally. Record native calls, token estimates,
time to a semantically useful result, grading time and overall elapsed time;
desktop timing stays unknown.

Pushback: one successful fixed feature cannot establish production reliability,
arbitrary-project support or a causal model ranking. The scripted steering
assertion can overlap a planner's own checks; it does not prove response to a
novel human requirement. Critique transfer is still separately logged as O-28.
Do not expand this run into that repair or a repeat-until-green campaign. Task 8
acceptance and release remain distinct from this experiment's outcome.
