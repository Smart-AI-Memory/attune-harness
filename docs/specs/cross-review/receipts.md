# Cross-review advisory receipts

These reviews inform human triage and do not gate merges, CI, or commands.

The Sol review used the native Codex subagent transport with `gpt-5.6-sol`; the moderator is Codex / GPT-6 (exact deployed model ID not exposed; see the identity correction in the artifacts). The module records provider seats, so its original `host=codex, self_review=true` flag is preserved. The separate native-agent model record establishes the requested model pairing; this is not a cross-provider review.

Raw results, verbatim manifests, central probes, and actual board-post receipts: [Sol review artifacts](../../receipts/cross-review-sol-20260916/). The first manifest is partial; the following scoped pass covers exactly its omitted paths. A Git binary marker does not constitute inspection of the SQLite history contents.

| Date | Seat | Target / independence | Manifest | Findings | Disposition |
| --- | --- | --- | --- | --- | --- |
| 2026-09-16 | codex | staged changes; host=codex, self_review=true, claude_auth=n/a | 29 sent / 30 omitted | 3 (findings) | 2 real — centrally reproduced with offline providers; left open for a separate fix. rejected — claim: "The new Voyage tool is advertised as uploading data and incurring costs but still publishes `read_only_hint=True`; an MCP client that auto-approves read-only tools can therefore send the query and selected source passages to Voyage and incur charges without treating the call as side-effecting." — reason: Not accepted as a demonstrated authorization bypass: the explicit allow_provider flag still gates dispatch. A central probe without it raises FeatureUnavailable with zero provider calls. The tool separately sets open_world_hint=True and describes uploads and costs. MCP readOnlyHint describes environmental modification, not a cost/confidentiality guarantee; no concrete client bypass was demonstrated. See https://modelcontextprotocol.io/specification/2025-11-25/schema#toolannotations. |
| 2026-09-16 | codex | staged changes — SCOPED to 30 path(s): README.md, docs/code-first-rag-receipt.md, docs/code-first-rag.md, docs/communication-grammar-review.md, docs/design-code-first-rag.md, docs/design-documentation.md, docs/design-voyage-100-question-evaluation.md, docs/design-voyage-answer-accuracy.md, docs/design-voyage-full-code-evaluation.md, docs/design-voyage-retrieval.md, docs/documentation-workflow.md, docs/harness-api-budget.md, docs/integrated-rag-architecture.md, docs/voyage-100-question-evaluation.md, docs/voyage-accuracy-receipt.md, docs/voyage-answer-accuracy-calibration-2026-09-15.md, docs/voyage-answer-accuracy-preparation.md, docs/voyage-full-code-evaluation.md, docs/voyage-latency-assessment-2026-09-15.md, docs/voyage-rag-session-starter.md, docs/voyage-retrieval-receipt.md, docs/voyage-retrieval.md, experiments/voyage/README.md, experiments/voyage/evaluate.py, experiments/voyage/tuning.json, requirements-voyage.lock, scripts/check_code_rag_host.py, scripts/qualify_platform.py, scripts/qualify_voyage_mutations.py, scripts/voyage_smoke.py; host=codex, self_review=true, claude_auth=n/a | 30 sent / 0 omitted | 4 (findings) | 2 real — evaluator exit semantics and optimized qualification false-pass centrally reproduced; left open for a separate fix. rejected — claim: "The report’s raw results, receipt audit, behavior tests, and installed-code audit all link into unstaged `.pilot` artifacts; a committed clone therefore loses the evidence needed to audit the exact 100-question and 200-call qualification claims." — reason: The local raw evidence files all exist and the report scores match summary.json. Project guidance explicitly preserves frozen raw campaigns locally and ignores .pilot in Git. Clone portability is a real limitation, already part of this local evidence policy, not a demonstrated defect in this change. No published reproducibility guarantee is made. rejected — claim: "The ADR first says the qualified in-process plugin supersedes the separate-stdio recommendation, then mandates stdio and defers the in-process adapter until later qualification; maintainers receive mutually exclusive deployment decisions from the same accepted document." — reason: Lines 39-47 explicitly date the update and say that the qualified in-process adapter supersedes the initial separate-stdio recommendation. The lower paragraph describes that initial recommendation. Historical text could be labeled more clearly, but the current decision is explicit; no contradictory current requirement was established. |

## Fix follow-up — September 16, 2026

The four real findings in the historical review rows above are now fixed in the
workspace: rerank budget preflight, interrupted plugin lifecycle, evaluator exit
status, and host checks under optimization. The [fix receipt](../../sol-review-fix-receipt.md)
records implementation, regression and mutation evidence. This is implementation
verification, not a new Sol review; the original replies and triage remain preserved.
