# Decisions

- D1: Patrick's “proceed with your recommended first three tests” accepts the three
  previously enumerated steps. Carry that authorization forward without new intake.
- D2: A repair-to-test provenance link is a necessary first-priority journey
  dependency. Other specialist generation/maintenance handoffs remain second.
- D3: No duplicate parser, workflow engine, gate store or model router. Use actual
  owning engine contracts and current receipt mechanisms.
- D4: Task 8 is implemented and executed, but not accepted. Original60 and repaired8
  stay distinct. Human grading was a frozen campaign choice, not a universal spec
  mandate. Amending it or admitting a fresh targeted trial requires a concrete
  decision; earlier declined repeats are not implicitly revived.

- D5: The installed representative journey is executor-led. Assessment→repair
  scope selection and test→Spec publication remain explicit executor handoffs;
  only repair→test is a new production evidence link. Do not describe this as an
  autonomous plan/build implementation or real human acceptance study.
- D6: Independent review reproduced probe and reviewer summary/journal disagreement.
  Both are repaired at the new handoff boundary and covered by failure-sensitive
  checks. This is persisted-evidence consistency, not hostile-local-process security.
- D7: Shared AI memory, Harness MCP2.2 services and AI code-retrieval bridge use
  separate dependency profiles. A missing optional Voyage dependency in the memory
  profile is disclosed and corrected by selecting the existing matching environment,
  not by weakening checks or changing packaging dependencies.
- D8: Patrick's “A repair and qualify the spec handoff” expands the memory
  documentation slice to repair the reproduced stale-approval defect. The
  [bounded correction](../../spec-test-handoff-results.md) is locally qualified:
  Harness owns the typed evidence check and the existing Spec adapter enforces it
  at publication and completion. D5's explicit executor boundary remains; no
  automatic plan/build or native semantic acceptance is claimed. Additional paid
  trials, live activation and release remain outside this authorization.
