# Connected capability map — 2026-09-17

This completes the inventory step of Patrick's approved sequence. Preserve every
useful capability; distinguish implemented connections from planned ones. The
[initial independent map](../../receipts/connected-journey-qualification/connected-journey-map-review.json)
binds the pre-change evidence. The implemented repair-to-test edge supersedes its
missing-edge finding. Final executed counts belong in the results report.

| Capability / entry point | Authority and persistent owner | Connection and remaining qualification |
|---|---|---|
| Human goal, `review --goal` | Accepted task request binds goal, evidence, participants and permissions; RunStore/TaskExecutionStore own progress | Solo or independent assessment dispatches configured roles. Retrieval and verification run as host operations. Narratives remain unverified; no automatic repair grant |
| Agents and bounded teams | Accepted registry and assignments; distinct worker/reviewer contexts; RecoveryCursor journals operations | Existing solo and independent-review patterns execute. Missing/rejecting participants fail visibly. Adaptive team selection, general DAG scheduling and distributed ownership are not implemented by this slice |
| `fix` | Accepted dedicated checkout, existing-file scope and immutable probe; host owns writes | Worker proposes replacements; host executes fail-before/pass-after checks and required review. Acceptance is limited to probe scope. Create/delete/rename and linked worktrees remain outside this effect profile |
| `test --from-task` (new edge) | New, separately accepted pytest task references the immutable completed repair | Host derives checkout/actual replacement paths and binds request, checkpoint, artifact and probe. Journaled probes/reviewer responses must agree with summaries. Stale/missing producer blocks current success. Standalone `test --project --scope` remains |
| `status`, `resume`, reconciliation, transfer, cancellation | Existing per-task journal and current checkpoint | Completed operations do not repeat. Unknown effects require explicit resolution; tests cannot be declared safe read-only retries. No aggregate transaction spans the verbs |
| Spec human decisions | Existing AI Spec workspace/collector; trusted executor publishes task results; saved Spec state retains accepted evidence | Actual installed collector tested with synthetic current/stale/replayed decisions. Failed tests are published as high-severity evidence and ordinary approval is refused. Risk acknowledgement remains an explicit existing human option. Product CLI does not automatically publish into Spec; executor owns that handoff |
| Assessment → repair | Human/executor selects goal, scope and acceptance criteria | Demonstrated as an explicit handoff in the installed journey, **not** a shared cross-phase work identity or automatic finding-to-patch planner |
| Memory in Harness and AI | Shared MemoryHost/WorkerStore; configured roots, ownership and host routing; AI/Harness CLI and AI MCP entry points | Existing content, corrections/deletions/context refresh, review and local accounting preserved. Native Luna worker/active-lead delegation remains unqualified; project ranking is not general access isolation |
| Local retrieval / verification | Pinned RAG/Verify libraries; corpus/context evidence and task grants | Wired into assessment; source and installed service tests exercise actual libraries. Verify 0.6.0 is the qualified Harness version |
| Voyage code retrieval | Accepted configuration/index generations, provider journal and evidence references | Existing configured retrieval connects to assessment and retrieval-tool sessions. Current checks use synthetic provider responses/local index fixtures; they add no new live Voyage/model performance claim |
| Extensions | Installed manifest/artifact digest, enable/disable/remove state, call-time grants | Data-only retrieval extension lifecycle is wired and tested. General executable plugin effects are not implied; plugin expandability remains an intended direction |
| Harness MCP | Launcher-selected principal, accepted retrieval grants, finite session budget | Actual stdio MCP 2.2.0 profile tested separately. AI integrated profile uses MCP 1.29.1; mutually exclusive pins cannot be advertised as one combined profile |
| Agent-to-agent transport | LocalPeer/A2AExchange owns endpoint, remote task and operation correlation | Loopback protocol success, failure, cancellation and reconnect exercised with real synthetic server. This does not qualify remote authenticated production teams or combine its store with the durable CLI task store |
| Native Claude/Codex adapters | Explicit accepted participant configuration and native transport | Software response/protocol controls tested. Original Task 8 and corrected follow-up remain separate evidence; broad native quality is still unaccepted |
| Legacy specialized workflows | Existing review, retrieval, triage, economics, GitHub checks and specialist test owners | Retained. Complete command catalog remains available. Specialist test generation/repair handoffs remain second priority unless a concrete first-priority dependency appears |
| `plan`, `build`, prompt/XML/spec authoring selection | Scoping draft; no production lifecycle owner yet | Still intended, not executable. Shared work identity, accepted-spec import, feature creation and cross-phase steering need the bounded plan/build design. This inventory does not authorize its unapproved eight-task draft |
| `ship`, `reflect`, full host UX | Intended further journeys and existing host affordances | No new Harness executable verbs added here. Native form lifetime/hover concern remains a separately logged host experiment, with durable text fallback |

The installed representative path proves that the current engines can cooperate
through an executor, with a production repair-to-test evidence link. It does not
prove an autonomous end-to-end plan/build product. This distinction keeps useful
features and makes the next missing connections concrete.
