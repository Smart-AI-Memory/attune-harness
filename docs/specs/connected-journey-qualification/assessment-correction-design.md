# Bounded assessment correction, revision 2

2026-09-17. Patrick authorized “do the next bounded correction.” Apply the selected
paragraph to a new experimental contract; preserve the completed comparison's
runner, fixtures, grades and receipts. Production adoption remains unqualified.
Done for this correction: exact wording delivered through installed task intake,
complete assessor evidence retained for grading, explicit disposition controls,
offline regression evidence and a concrete next comparison with estimated cost.

The scratch probe at
`/private/var/folders/5t/bzsz5qd17pd7h_wwwd5qjbnw0000gn/T/assessment-correction-probe-oxp17_sg/probe.json`
used the qualified installed wheel and actual task acceptance/retrieval/verification
path with an injected response. It completed with one intercepted native request;
the entire new paragraph and reference reached the assessor. The host checked one
relative link, with `semantic_ran=false`. No subprocess or inference ran. This
confirms transport, not improved judgment.

Create a separate fixture and offline preparation script reusing the existing
assessment and repaired evidence-projection functions. Four fresh Cedar cases
cover an unsupported receipt-signature assertion, an accurately disclosed unknown,
a real contradiction about receipt availability with a missing explicit owner,
and a fully supported statement satisfying that owner requirement. Rehearse the
eight previous cases separately;
they are development regressions, not fresh holdouts or model-quality evidence.
Keep the baseline unchanged. Freeze both instruction arms and prospective keys.

Clarify grading prospectively: a definite unsupported assertion needs qualification,
removal or evidence before acceptance. Merely noticing the evidence gap while
making correction optional is incorrect disposition. `critical_miss` measures
failure to notice an operational defect; it is false when the gap was noticed but
its disposition was wrong. Both fail the correctness floor. Preserve uncertainty:
missing support establishes neither the assertion nor its opposite. Add controls
for optional-only correction, an accurate unknown and an invented opposite fact.
Send complete documents, reference paths/hashes and actual host checks to graders;
withhold answer keys, arm labels, model identities and prior grades.

Proposed qualification: four fresh cases × two arms × two lead families = 16
assessments, plus eight independent grading batches, two distinct cases per batch.
Sol grades Fable; Fable grades Astra. Require all candidate cases correct, no
unsupported allegations, all controls passing and no matched regressions; retain
disagreements for review. One repetition provides direction, not a reliability
estimate or production acceptance. Prepare a proposal, never a fabricated live
admission. The original allocation has no Astra slots left; any new allocation
must be explicit before dispatch. Compute the estimate from retained usage.

Rejected: editing frozen evidence, retesting only Canada, treating injected replies
as model improvement, rewriting old grades under a new rubric, another generic
runner, and bundling host rendering changes into the instruction comparison.
Existing host receipts already contain the mechanical checks; rendering them alone
would not prevent overclaiming in model prose. O-16 remains a separate opportunity.

Patrick subsequently approved the exact proposed allocation in the saved form
reply. The execution adapter reuses the prior meter, subscriptions, admission
checks and raw-response audit, with 16 assessments followed by eight grading
batches. The lead review is saved before independent grading and hash-bound to
that phase. Forty offline checks cover this adapter and the prior controls before
live dispatch. No frozen preparation or historical result is rewritten.
