# Connected journey qualification — authorized sequence

Patrick approved mapping intended capabilities, qualifying a representative
installed journey, then extending checks across the remaining journeys on
2026-09-17. Priorities: complete agent/team/workflow/human-control journeys first;
specialist testing handoffs second, except an actual dependency of the first.

Outcome: demonstrate the installed assessment → bounded repair with independent
review → tests → existing Spec human decision/recovery path, with explicit links
between its evidence and current authority. Maintain a capability-to-journey map
that includes unimplemented and native-unqualified capabilities. A passing local
journey cannot become a claim that every intended product capability is ready.

Requirements:

- Preserve useful capabilities, accepted controls and dirty work. Reuse the task,
  recovery and Spec grammar mechanisms; introduce no general workflow/gate engine.
- Test a completed repair through a host-derived checkout/scope and bound producer
  identity, checkpoint, artifact and probe. Reject failed/unfinished/stale producers
  and material input overrides. A linked test cannot grant repair or human approval.
- Exercise actual CLI/process participants and installed artifacts. Retain normal,
  wrong-output, unavailable-participant, stale-action, failed-test and interrupted
  outcomes, with no duplicate effects or false promotion of model narratives.
- Use the real existing Spec collector for simulated human-control cases. These
  are interaction-contract checks, not actual user acceptance or a usability study.
- Extend checks across current memory, retrieval, extensions and supported host
  paths. Record missing implementation, unsupported profiles and environment
  blockers explicitly; do not subtract them from the intended inventory.
- Bring native Task 8 forward within this sequence. Reuse original 60 and corrected
  eight-case evidence; preserve the original failed comparison and no-repeat
  decision. Prepare remaining quality/grading decisions before any new live trial.

Done when the map, connected source/installed cases, relevant negative controls,
independent review closure and durable receipts exist, and the native qualification
follow-through has an explicit implementation/evaluation/decision schedule.
Native promotion, fresh provider spending, active plugin activation and release
are not inferred from this software-qualification authorization.
