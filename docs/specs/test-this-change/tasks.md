# Test this change — approved first slice

Patrick approved the stated implementation boundary on 2026-09-17. Execute the
following dependent units, retaining evidence and pausing for serious unresolved
findings. Each unit's receipt must distinguish implemented behavior from support
limits. The previous memory and release-readiness ladders remain complete.

1. Implement change capture, conservative test selection, durable testing intake
   and grammar preview. Probe scope containment, duplicate basenames, indirect
   callers, broad fallback and stale acceptance. Preserve unrelated dirty files.
2. Connect supervised pytest execution, observed execution counts, output artifacts
   and the existing task/recovery store. Probe failure/empty/collection-only,
   interrupted/large-output, stale results and crash/resume behavior.
3. Wire the test verb and existing controls; qualify the complete CLI, installed
   consumer and compatibility. Run the real Spec case, negative/protection-removal
   probes, independent review, then publish exact results and remaining limits.

Expected files: new testing-policy modules and tests; small routing changes in
the existing CLI/task files; documentation. No dependency or packaging extra change
is planned. Existing specialized workflows remain untouched.

Completion: all three units implemented and verified. Exact results and limits are
recorded in [the implementation receipt](../../test-this-change-results.md).
Executor receipts are saved with the existing Spec state API in the XML plan.
