# Fresh journey qualification — September 18, 2026

**The fresh complete journey is not yet qualified.** Planning, the exporter and
the CLI pass; one original generated-test assertion fails. Harness records
`needs_revision`, rejects an incomplete completion handoff and resumes the failed
record without another participant call. Task 8 remains open.

Patrick explicitly authorized **40 additional credits**. Five native calls used
**4.701455 estimated Codex credits** and zero new API dollars. The final-review
call is unused. This six-call campaign is closed with no retry, replacement,
fallback or repaired original response. The prior 7.744460-credit campaign stays
separate. Estimates are token-based, not verified account deductions.

## Original outcomes

| Stage | Result |
|---|---|
| Luna plan | Preserves the corrected goal, evidence, scope, empty choices, byte-compatible defaults and disclosed performance uncertainty. |
| Astra critique | No findings. Restating the protected API/envelope remains advisory. |
| Luna exporter | Exact record envelope, ordering and evidence preservation pass the protected check. |
| Synthetic Spec correction | Old authority invalidated; current revision reaccepted; replay rejected; completed exporter preserved. |
| Luna CLI | All four protected checks pass, including captured pre-change stdout for populated and empty input. |
| Luna supplemental tests | Three pass; the mixed-status assertion raises KeyError. |
| Final reviewer and completed handoff | Not reached; no native completion or stale-completion claim. |

All five original replies pass their role contracts. The CLI's Unicode escape
spellings preserve the original characters; all non-main AST nodes match the
captured implementation. Protected execution independently checks compatibility.
The generated complete-receipt reconstruction assertion is present and passes.
Its mixed-status assertion incorrectly reads:

```python
[finding['status'] for finding in records[1:]]
```

Status lives inside each record's finding member. Source inspection catches the
error before execution; the original tests confirm it through the protected
supplemental runner. Module origins are correct. This is a generated-test defect,
not a recurrence of the repaired import or compatibility handoffs. Five probe
runs execute 13 tests including repeats: 12 pass and one errors. Protected inputs,
unrelated dirty work and completed exporter bytes remain intact.

## Separate diagnosis and controls

In a disposable copy only, changing the assertion to
`record['finding']['status']` produces **4/4 supplemental and 4/4 protected
passes**. Original source, replies and journal remain unchanged. This establishes
a narrow test error; it does not qualify native repair or Spec repair/resume.
The diagnostic initially used the wrong module for completed_source; importing
its existing task_handoff owner corrected that diagnostic without product edits.

An actual completed-source request rejects the incomplete build. Reopening the
failed record returns needs_revision with a participant factory that raises if
called. No additional participant invocation occurs. Twenty preflight checks and
both lifecycle checks passed before dispatch; all 1,355 earlier receipt files
remain unchanged. Four earlier passed review controls are retained, not rerun.

## Cost and timing

| Role | Requested model | Estimated credits | Dispatch to validated payload |
|---|---|---:|---:|
| Planner | Luna | 0.160685 | 33.45 s |
| Critic | Astra | 4.048550 | 17.60 s |
| Exporter worker | Luna | 0.107195 | 23.04 s |
| CLI worker | Luna | 0.183355 | 69.90 s |
| Supplemental-test worker | Luna | 0.201670 | 67.13 s |

The useful plan returns in 33.45 seconds; its durable semantic assessment is
saved at 57.95 seconds after the first request and planning grade at 77.98 seconds.
Native validated-response times total 211.12 seconds. Fixture creation through
stopped result is approximately 477.42 seconds, including inspection, orchestration,
synthetic steering and local diagnosis. Wall intervals use retained file timestamps.
Desktop presentation and real human-response time are unknown. Unequal incomplete
journeys and cache differences do not support model speed/cost rankings.

Attribution uses requested models and retained argv; backend model identities are
not reported. Each invocation reports one completed turn and no observed tool-use
item. Startup context remains uncontrolled. The session assistant grades labeled
replies; this is neither blind nor independent semantic review. The steering is
synthetic and does not qualify real human interaction or broad changed-intent
adaptation.

## Next bounded opportunity

Use this retained failure to qualify the existing repair → retest → resume
handoff. Preserve the successful exporter/CLI, correct only the failed test,
invalidate old authority, require current acceptance, rerun both test sets and
obtain native final review before accepting completed evidence. Establish the
continuation locally before proposing another paid sequence. The current trial
remains failed; its unused call is not silently reassigned. Repeatedly starting
whole journeys until one passes would hide the recovery boundary we need to test.

Evidence: [summary](receipts/plan-build-fresh-journey-native-2026-09-18/summary.json),
[ledger](receipts/plan-build-fresh-journey-native-2026-09-18/ledger.json),
[actual probes](receipts/plan-build-fresh-journey-native-2026-09-18/protected-and-generated-probes.json),
[failed-resume checks](receipts/plan-build-fresh-journey-native-2026-09-18/journeys/luna-routine/failed-resume.json),
[separate diagnostic](receipts/plan-build-fresh-journey-native-2026-09-18/local-repair-diagnostic.json).
