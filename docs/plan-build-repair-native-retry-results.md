# Native guided repair — unchanged proposal, September 18, 2026

**The guided repair fails.** With corrected host launch access, one Luna call
returns the original generated test byte-for-byte unchanged. The requested
nested-field correction is absent. The call uses **0.179815 estimated Codex
credits** and takes **39.05 seconds**. No file effect or Astra review occurs.
The two-call allocation is closed with its review call unused. The prior startup
failure and its two-credit planning reserve remain separate from measured usage.
Neither estimate is a verified account deduction.

The returned schema fields and preimage digest are correct. However, the effect
validator rejects unchanged replacements, and the original faulty assertion is
still present. Parent inspection stops the trial before execution. Separate local
probes of the unchanged, previously inspected source confirm three supplemental
passes and the same KeyError; all four protected checks pass. Those diagnostics
do not apply the proposal or resume the stopped build. The complete original
response, raw process output, failed owner and all 12,534 earlier receipt files
remain intact.

## Host and handoff findings

The saved worker result exposes a local inspection defect: the operation cursor
persists the response and pauses before decoding it, but read_task then rejects
the whole owner because the saved proposal is invalid. Invalid participant data
must remain inspectable as failed evidence without ever authorizing effects or
completion. Qualify a bounded fix for terminal rejected replies and no-repeat
resume; keep malformed host metadata and effects after an invalid reply rejected.

The repair turn also retains the original “add supplemental tests” objective.
It includes the nested-access instruction as a check, but omits the actual prior
failure output. This is a concrete handoff clarity gap, not proof of why Luna
copied the file. Clarify the accepted repair objective with the observed failure
and preserve the test expectations before another paid experiment. Do not add a
stronger router or claim that the original trial succeeded.

Task 8 remains open. Continue these bounded local corrections under the existing
authorization. No more native calls are authorized by the stopped allocation.

Evidence: [summary](receipts/plan-build-repair-native-retry-2026-09-18/summary.json),
[original proposal](receipts/plan-build-repair-native-retry-2026-09-18/calls/luna-routine-worker-2/decoded.json),
[inspection](receipts/plan-build-repair-native-retry-2026-09-18/journeys/luna-routine/luna-routine-worker-2-inspection.json),
[actual diagnostic tests](receipts/plan-build-repair-native-retry-2026-09-18/unchanged-source-diagnostic.json).
