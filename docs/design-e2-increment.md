# E2 — design and criteria frozen before evaluation

2026-09-14. Execute the authorized Phase 4 E2 comparison, not a new runtime or
release. Done when all six states × two adapter types × two policies have raw
receipts, independent invocation outcomes and an adopt/revise/inconclusive
disposition. Preserve the qualified wheel and production call-time checks.

[Protocol revision 1](../experiments/e2/protocol.json) fixes the matrix, policies,
oracle and criteria before the evaluator is implemented. The baseline wheel
matches every production source module and the previous 427-test receipt.
The disposable `/tmp/harness-e2-disposable.py` made a real local retrieval,
then changed skill bytes: the retained result kept its original artifact digest
while current discovery produced a different digest. That establishes a usable
artifact-change signal, not availability prediction. The probe is retained with
the experiment receipts.

Compare the existing command-review boundary (independent subprocess participant,
real coordinator/tool dispatch) and MCP 2.2.0 stdio with a 2026-07-28 independent
client. Each policy/state cell starts from fresh files and a successful observation.
Apply the seeded state, save a decision in a separate process, then invoke an
independent fresh oracle process. Save raw calls, records, descriptor differences
and the decision before scoring. Both conditions use the same fixture data and
fault operation; there are no model calls or account operations.

The declaration-only condition explicitly tests the naive inference "the manifest
lists it, so predict usable". Existing Harness never calls that verified
availability, and the report must not misrepresent it as a shipped defect. The
candidate adds a version/scope-bound successful-call cache and tests promotion to
verified availability. Its key includes the artifact, installed dependency and
adapter versions, schemas, accepted registry/grants and source snapshot. It cannot
read state labels, fault controls or oracle outcomes. A mutable backend failure
without changed metadata is a material case; do not redefine it away if the
candidate misses it. The oracle establishes usability through the actual adapter,
including checking result/source hashes independently of the policy.

Use an isolated no-rag environment made by copying the qualified environment's
packages except attune-rag code and distribution metadata, and verify actual
absence. Runtime breakage is an explicitly injected exception at the retriever
boundary inside the fixture process; no installed package file is changed.
Upgrade follows the real disable/replace/enable path and accepts the new artifact.
Permission denial changes accepted grants and attempts the target anyway.

Count false usable predictions separately from false verified claims. Separate
rejection from deferral: upgraded working code may need a fresh qualification;
report that delay, then perform the preregistered requalification check. Report
every seeded case, not just the aggregate. No time/cost/speed claim is planned;
different clean processes supply the baseline and candidate evidence.

Keep the candidate evaluator under `experiments/e2/`. Reject adding a production
capability manager before it earns adoption. If it fails, retain call-bound
observations and current guards, record revise, and name the narrower future
design. A fixture fault is not automatically a production bug. No source/test
mutation receipt is owed for new experiment code; existing production paths are
not being edited. Test the evaluator with misleading, stale and mismatched
observations and independently check its matrix accounting.
